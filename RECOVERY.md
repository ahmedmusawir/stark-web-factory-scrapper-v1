# Recovery State

Last action: **wf-scrapper-abm P0 (task zero) reported — PARTIAL, host rate-limited us** (2026-09-18 20:35 +06). Branch `wf-scrapper-abm` @ `9162873`. A env DONE (pyenv 3.12.3 built; `pip install -r requirements.txt` gave freeze ≠ lock on 16 transitives → installed from `requirements-lock.txt` → freeze == lock, 98 lines; pip check clean; Chromium 1169 installed). B regression **54 passed**. D REST probe: `/wp-json/` 200, `wp/v2/pages?per_page=1` **429**, `wp/v2/posts?per_page=1` **429** (edge burst limiter `_atomic_bur`, no Retry-After). C smoke **NOT RUN**: discovery GET `/sitemap.xml` → **429** twelve minutes later; no `discovered_pages.json`, no run folder. Nothing retried. Report: `agent_docs/ACTION/wf-scrapper-abm/EVIDENCE/p0/P0_REPORT.md`.
Pending: **Director decisions** (report, last section): (1) approve a re-probe + smoke after cool-down (≥ 1 h, smoke first, REST GETs ≥ 10 s apart, stop at first 429); (2) route erratum candidates to Architect — REST pacing (E-4), run-level vs stream-level stop (E-5), 429 during sitemap fallback (E-11), AC-001 freeze==lock only via lock file (A-1); (3) run P1 now with Contracts §2.5 open, or wait. No plan awaiting approval. Contracts §2.5 REST field names remain UNCONFIRMED — C4 precondition not met.
Next step: on Director word → re-run P0 parts C and D only (A and B are done; **do not rebuild the venv, do not rerun anything "to be sure"**), append results to the P0 report. Then "Run P1." → whole-campaign Plan Mode readback (`ABM_BUILD_INSTRUCTIONS.md` §6 P1). Live requests made so far: 4 (3 REST + 1 sitemap); no crawl budget used. Read order after a crash: `agent_docs/ACTION/wf-scrapper-abm/CLAUDE.md` → this file → P0 report → `agent_docs/SESSIONS/session_2026-09-18.md`. Standing rulings: git Operator-only; R1–R8 closed (`ABM_RULING_SHEET.md`); no new dependencies; no stealth; always `venv/bin/python`; pins frozen (crawl4ai 0.9.3 / playwright 1.52.0); contract files in the module folder are frozen — errata, not edits.
