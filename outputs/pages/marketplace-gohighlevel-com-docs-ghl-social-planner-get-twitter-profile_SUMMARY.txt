Here is a structured summary of the provided API documentation:

### 1. ENDPOINT NAME:
Get Twitter profile

### 2. CATEGORY:
Social Planner | Oauth | Twitter

### 3. HTTP METHOD:
GET

### 4. ENDPOINT URL:
`/social-media-posting/oauth/:locationId/twitter/accounts/:accountId`

### 5. FULL URL:
`https://services.leadconnectorhq.com/social-media-posting/oauth/:locationId/twitter/accounts/:accountId`

### 6. REQUIRED PARAMETERS:
*   **Header Parameters:**
    *   `Version` (string): API Version. Possible values: `2021-07-28`
*   **Path Parameters:**
    *   `locationId` (string): Account Location Id (Example: `w37swmmLbA02zgqKPpxITe2`)
    *   `accountId` (string): Account Id (Example: `w37swmmLbA02zgqKPpxIT`)

### 7. OPTIONAL PARAMETERS:
Not specified in documentation

### 8. AUTHENTICATION:
Implied bearer token/access token is required for authorization, as indicated by the `401 Unauthorized` response with message "Invalid token: access token is invalid". The endpoint path includes `oauth`, suggesting OAuth 2.0 is the underlying mechanism.
Required scopes: Not explicitly specified in documentation.

### 9. RESPONSE FORMAT:

**200 OK - Successful response (application/json)**
```json
{
  "success": true,
  "statusCode": 200,
  "message": "Fetched Twitter Account",
  "results": {
    "profile": [
      {
        "id": "ZHl1OWpfZEMyeWc5UjJOOU51RVNOal",
        "name": "Twitter Account name",
        "username": "sample_user",
        "avatar": "ZHl1OWpfZEMyeWc5UjJOOU51RVNOal",
        "protected": true,
        "verified": true,
        "isConnected": true
      }
    ]
  }
}
```
*   **Schema Details:**
    *   `success` (boolean): Success or Failure (required)
    *   `statusCode` (number): Status Code (required, Example: `200`)
    *   `message` (string): Message (required, Example: `Fetched Twitter Account`)
    *   `results` (object): Requested Results
        *   `profile` (array of objects): Twitter Profile Details
            *   `id` (string): ID of profile (Example: `ZHl1OWpfZEMyeWc5UjJOOU51RVNOal`)
            *   `name` (string): Name of profile (Example: `Twitter Account name`)
            *   `username` (string): Username of profile (Example: `sample_user`)
            *   `avatar` (string): Avatar of profile (Example: `ZHl1OWpfZEMyeWc5UjJOOU51RVNOal`)
            *   `protected` (boolean): Is protected (Example: `true`)
            *   `verified` (boolean): Is verified (Example: `true`)
            *   `isConnected` (boolean): Is connected (Example: `true`)

**Other Responses:**
*   **400 Bad Request (application/json)**
    *   `statusCode`: 400
    *   `message`: "Bad Request"
*   **401 Unauthorized (application/json)**
    *   `statusCode`: 401
    *   `message`: "Invalid token: access token is invalid"
    *   `error`: "Unauthorized"
*   **422 Unprocessable Entity (application/json)**
    *   `statusCode`: 422
    *   `message`: `["Unprocessable Entity"]`
    *   `error`: "Unprocessable Entity"

### 10. PURPOSE:
This endpoint retrieves the profile details of a connected Twitter account for a given location and account ID.

### 11. TAGS:
Social Planner, Twitter, Profile, Get, Account, OAuth, Deprecated, Social Media Posting.

### 12. DEPRECATION STATUS:
**Deprecated:** This endpoint has been deprecated. As of December 4, 2024, X (formerly Twitter) is no longer supported.

### 13. CODE EXAMPLES:
Code examples are available for the following languages:
*   cURL
*   nodejs
*   python
*   php
*   java
*   go
*   ruby
*   powershell