Here is a structured summary of the API documentation:

### 1. ENDPOINT NAME:
Search Opportunities

### 2. CATEGORY:
Opportunities

### 3. HTTP METHOD:
POST

### 4. ENDPOINT URL:
`/opportunities/search`

### 5. FULL URL:
`https://services.leadconnectorhq.com/opportunities/search`

### 6. REQUIRED PARAMETERS:

*   **Header Parameters:**
    *   Authorization (string): Access Token (Example: `Bearer 9c48df2694a849b6089f9d0d3513efe`)
    *   Version (string): API Version (Possible values: `2021-07-28`)

*   **Body Parameters:**
    *   locationId (string): Location Id (Example: `i2SpAtBVHSVea1sL6oah`)
    *   query (string): Not explicitly described, but shown as `string` in example body.
    *   limit (number): Not explicitly described, but shown as `0` in example body.
    *   page (number): Not explicitly described, but shown as `0` in example body.
    *   searchAfter (string[]): Not explicitly described, but shown as `["string"]` in example body.
    *   additionalDetails (object): Not explicitly described.
        *   notes (number): Not explicitly described, but shown as `0` in example body.
        *   tasks (number): Not explicitly described, but shown as `0` in example body.
        *   calendarEvents (number): Not explicitly described, but shown as `0` in example body.
        *   unReadConversations (boolean): Not explicitly described, but shown as `true` in example body.

### 7. OPTIONAL PARAMETERS:
Not explicitly specified in documentation. All listed request body parameters are marked `required`.

### 8. AUTHENTICATION:
*   **Auth Method(s):** `OAuth Access Token`, `Private Integration Token`
*   **Token Type(s):** `Sub-Account Token`
*   **Scheme:** Bearer (JWT)

### 9. RESPONSE FORMAT:

*   **200 OK (application/json):**
    *   **opportunities** (object[]): An array of opportunity objects.
        *   **id** (string)
        *   **name** (string)
        *   **monetaryValue** (number)
        *   **pipelineId** (string)
        *   **pipelineStageId** (string)
        *   **assignedTo** (string)
        *   **status** (string)
        *   **source** (string)
        *   **lastStatusChangeAt** (string)
        *   **lastStageChangeAt** (string)
        *   **lastActionDate** (string)
        *   **indexVersion** (string)
        *   **createdAt** (string)
        *   **updatedAt** (string)
        *   **contactId** (string)
        *   **locationId** (string)
        *   **contact** (object)
            *   **id** (string)
            *   **name** (string)
            *   **companyName** (string)
            *   **email** (string)
            *   **phone** (string)
            *   **tags** (string[])
        *   **notes** (string[])
        *   **tasks** (string[])
        *   **calendarEvents** (string[])
        *   **customFields** (object[])
            *   **id** (string, required)
            *   **fieldValue** (oneOf: string, object, string[], object[], required)
        *   **followers** (array[])
    *   **meta** (object)
        *   **total** (number)
        *   **nextPageUrl** (string)
        *   **startAfterId** (string)
        *   **startAfter** (number)
        *   **currentPage** (number)
        *   **nextPage** (number)
        *   **prevPage** (number)
    *   **aggregations** (object)

*   **400 Bad Request (application/json):**
    *   **statusCode** (number)
    *   **message** (string)

*   **401 Unauthorized (application/json):**
    *   **statusCode** (number)
    *   **message** (string)
    *   **error** (string)

### 10. PURPOSE:
This endpoint allows searching for opportunities based on combinations of advanced filters.

### 11. TAGS:
Opportunities, Search, Filter, Advanced Search, CRM, Lead, Pipeline

### Code Examples:
Code examples are available for: `curl`, `nodejs`, `python`, `php`, `java`, `go`, `ruby`, `powershell`.