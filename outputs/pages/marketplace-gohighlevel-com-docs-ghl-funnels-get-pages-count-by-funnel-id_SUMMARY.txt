Here is a structured summary of the provided API documentation:

### 1. ENDPOINT NAME:
Fetch count of funnel pages

### 2. CATEGORY:
Funnels

### 3. HTTP METHOD:
GET

### 4. ENDPOINT URL:
`/funnels/page/count`

### 5. FULL URL:
`https://services.leadconnectorhq.com/funnels/page/count`

### 6. REQUIRED PARAMETERS:
*   `locationId` (string): Not explicitly described, but inferred as the ID of the sub-account/location.
*   `funnelId` (string): Not explicitly described, but inferred as the ID of the funnel.

### 7. OPTIONAL PARAMETERS:
*   `name` (string): Not explicitly described, but inferred as a name to filter funnel pages by.

### 8. AUTHENTICATION:
*   **Auth Method(s):** `OAuth Access Token`, `Private Integration Token`
*   **Token Type(s):** `Sub-Account Token`
*   **Scheme:** `bearer`
*   **Bearer Format:** `JWT`
*   **In:** `header`
*   **Description:** Use the Access Token generated with user type as Sub-Account (OR) Private Integration Token of Sub-Account.

### 9. RESPONSE FORMAT:
*   **Status Code:** `200 OK`
*   **Content Type:** `application/json`
*   **Schema:**
    *   `count` (number, required): The total count of funnel pages.
*   **Example:**
    ```json
    {"count": 20}
    ```

### 10. PURPOSE:
Retrieves the count of all funnel pages based on the provided query parameters for a specific funnel and location.

### 11. TAGS:
Funnels, Pages, Count, Retrieve, Get, Funnel Pages, Analytics, Sub-Account, Location

### 12. CODE EXAMPLES:
Code examples are available for: `curl`, `nodejs`, `python`, `php`, `java`, `go`, `ruby`, `powershell`.