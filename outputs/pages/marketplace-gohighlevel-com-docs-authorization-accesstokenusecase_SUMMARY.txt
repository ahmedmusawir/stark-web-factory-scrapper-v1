Here is a structured summary of the provided documentation:

---

### 1. ENDPOINT NAME:
Access Token Generation: Agency vs. Sub-Account Scenarios

### 2. CATEGORY:
Authorization, OAuth 2.0

### 3. HTTP METHOD:
Not specified in documentation (This page describes a conceptual process, not a specific API endpoint that uses an HTTP method).

### 4. ENDPOINT URL:
Not specified in documentation (This page describes a conceptual process, not a specific API endpoint path).

### 5. FULL URL:
Not specified in documentation (The provided URL is for the documentation page itself, not an API endpoint).

### 6. REQUIRED PARAMETERS:
Not specified in documentation (This page discusses the scenarios influencing token type, not parameters for an API call).

### 7. OPTIONAL PARAMETERS:
Not specified in documentation (This page discusses the scenarios influencing token type, not parameters for an API call).

### 8. AUTHENTICATION:
*   **Method**: OAuth 2.0
*   **Token Types**: Agency-level Access Token, Sub-Account (Location-level) Access Token
*   **Context**: The type of access token issued depends on who installs the app (an agency or a sub-account) and the app's distribution settings.

### 9. RESPONSE FORMAT:
Not specified in documentation (This page describes a conceptual process, not the response of an API call).

### 10. PURPOSE:
This documentation explains how the type of Access Token (Agency-level or Sub-Account-level) is determined based on the user installing the app (agency or sub-account) and the app's distribution settings within the HighLevel platform.

### 11. TAGS:
Authorization, OAuth 2.0, Access Token, Token Generation, Agency, Sub-Account, Location, Authentication, App Installation, Distribution, Scenarios, HighLevel API.

---