Here's a structured summary of the TaskCreate Webhook documentation:

### ENDPOINT NAME
TaskCreate Webhook

### CATEGORY
Webhook

### HTTP METHOD
POST (This is the method Highlevel uses to send the webhook payload to your specified URL. It is not an endpoint you call directly.)

### ENDPOINT URL
Not applicable (This is a webhook sent *by* Highlevel to a URL you configure, not an API endpoint you call *on* Highlevel's platform.)

### FULL URL
Not applicable (This is a webhook sent *by* Highlevel to a URL you configure, not an API endpoint you call *on* Highlevel's platform.)

### REQUIRED PARAMETERS (Webhook Payload)
*   **type** (string): The type of the webhook event, which is "TaskCreate".
*   **locationId** (string): The unique identifier of the location (sub-account) where the task was created.
*   **id** (string): The unique identifier of the created task.
*   **assignedTo** (string): The ID of the user to whom the task is assigned.
*   **body** (string): The detailed description or content of the task.
*   **contactId** (string): The unique identifier of the contact associated with the task.
*   **title** (string): The title or subject of the task.
*   **dateAdded** (string): The date and time when the task was added (ISO 8601 format).
*   **dueDate** (string): The date and time when the task is due (ISO 8601 format).

### OPTIONAL PARAMETERS
Not specified in documentation (All parameters listed in the schema are included in the example payload, implying they are standard components of the webhook event.)

### AUTHENTICATION
Not specified in documentation (Webhook security typically involves signature verification, but specific methods or tokens are not mentioned here.)

### RESPONSE FORMAT
This documentation describes the payload *sent* by the webhook. It does not specify the expected response format *from* the receiving server, though typically a `200 OK` status code is expected to acknowledge receipt.

### PURPOSE
This webhook is called whenever a new task is created within the Highlevel system, providing details about the created task.

### TAGS
Webhook Events, Task, Create, TaskCreate, Event, Highlevel

### CODE EXAMPLES
Availability: Only JSON schema and an example JSON payload are provided. No code examples in languages like Python, Node, or cURL are present.