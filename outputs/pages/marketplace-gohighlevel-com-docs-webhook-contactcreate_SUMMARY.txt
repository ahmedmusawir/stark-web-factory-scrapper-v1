Here's a structured summary of the API documentation for the `ContactCreate` webhook:

### 1. ENDPOINT NAME
ContactCreate

### 2. CATEGORY
Webhooks, Contacts

### 3. HTTP METHOD
Not applicable. This document describes a webhook *event* payload that HighLevel sends to your configured endpoint, not an endpoint you call directly.

### 4. ENDPOINT URL
Not applicable. This is a webhook event. The URL would be *your* callback URL where HighLevel sends this payload.

### 5. FULL URL
Not applicable.

### 6. REQUIRED PARAMETERS (Webhook Payload Fields)
The schema defines the structure of the payload. All properties listed in the schema are potential fields in the webhook payload. The documentation does not explicitly mark fields as "required" or "optional" within the schema definition for the webhook payload itself. All fields present in the schema are listed below.

*   **type** (string): The type of the webhook event. (e.g., "ContactCreate")
*   **locationId** (string): The ID of the location where the contact was created.
*   **id** (string): The unique identifier of the created contact.
*   **address1** (string): The primary address line of the contact.
*   **city** (string): The city of the contact's address.
*   **companyName** (string): The name of the company associated with the contact.
*   **country** (string): The country of the contact's address.
*   **source** (string): The source from which the contact was added (e.g., "xyz form").
*   **dateAdded** (string): The date and time the contact was added (ISO 8601 format).
*   **dateOfBirth** (string): The contact's date of birth (ISO 8601 format).
*   **dnd** (boolean): Do Not Disturb status for the contact.
*   **email** (string): The email address of the contact.
*   **name** (string): The full name of the contact.
*   **firstName** (string): The first name of the contact.
*   **lastName** (string): The last name of the contact.
*   **phone** (string): The phone number of the contact.
*   **postalCode** (string): The postal code of the contact's address.
*   **state** (string): The state of the contact's address.
*   **tags** (array): An array of tags associated with the contact.
*   **website** (string): The website URL associated with the contact.
*   **attachments** (array): An array of attachments associated with the contact.
*   **assignedTo** (string): The ID of the user or entity the contact is assigned to.
*   **customFields** (array): An array of custom fields, each containing:
    *   **id** (string): The ID of the custom field.
    *   **value** (string|number|array|object): The value of the custom field.

### 7. OPTIONAL PARAMETERS
Not explicitly specified in documentation. All fields provided in the schema are listed above under "Required Parameters (Webhook Payload Fields)" as they form the potential payload structure.

### 8. AUTHENTICATION
Not specified in documentation for receiving the webhook. Typically, incoming webhooks require verification (e.g., signature checking) to ensure authenticity, but details are not provided here.

### 9. RESPONSE FORMAT
Not specified in documentation. For webhooks, the receiving server typically responds with an HTTP status code (e.g., `200 OK`) to acknowledge successful receipt of the payload.

### 10. PURPOSE
This webhook is called whenever a new contact is created within the HighLevel system.

### 11. TAGS
Webhook, Event, Contact, Create, Contact Management, Webhook Events

### 12. CODE EXAMPLES
A JSON example of the webhook payload is provided. No code examples (Python, Node, cURL) for handling the webhook are present.