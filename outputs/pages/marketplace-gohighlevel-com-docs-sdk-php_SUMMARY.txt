Here's a structured summary of the provided HighLevel PHP SDK documentation:

---

### 1. ENDPOINT NAME:
Search Contacts Advanced (Example API Call)

### 2. CATEGORY:
Contacts

### 3. HTTP METHOD:
POST (Inferred from the use of a request body `SearchBodyV2DTO` for search criteria; not explicitly stated.)

### 4. ENDPOINT URL:
Not explicitly specified (The SDK abstracts the exact API endpoint path, but it's part of the Contacts service).

### 5. FULL URL:
Not explicitly specified

### 6. REQUIRED PARAMETERS:
These parameters are part of the `SearchBodyV2DTO` request body used for the `searchContactsAdvanced` method:
*   `locationId` (string): The ID of the location to search contacts within.
*   `pageLimit` (integer): The maximum number of contacts to return per page.

### 7. OPTIONAL PARAMETERS:
Not specified in documentation for the `SearchBodyV2DTO` example.

### 8. AUTHENTICATION:
The HighLevel PHP SDK supports the following authentication methods:
*   **Private Integration Token**: Initialized using `'privateIntegrationToken'` in `HighLevelConfig`.
*   **OAuth 2.0**: Initialized using `'clientId'` and `'clientSecret'` in `HighLevelConfig`.
The SDK handles automatic OAuth token rotation. Specific scopes are not detailed in this document.

### 9. RESPONSE FORMAT:
The example shows the `$contactsResponse` object being `json_encode`d, indicating a JSON response format. A successful HTTP status code (e.g., 200 OK) is implied but not explicitly stated.

### 10. PURPOSE:
This documentation page introduces the official HighLevel PHP SDK, providing guidance on its installation, initialization with different authentication methods, usage for making API calls (demonstrated with a contact search example), session storage, and webhook processing.

### 11. TAGS:
PHP, SDK, HighLevel, API, Contacts, Search, OAuth, Webhooks, Private Integration Token, Session Storage, Installation, `api-client`, `gohighlevel`

### Code Examples:
PHP code examples are available for:
*   Composer installation.
*   Initializing the SDK with a Private Integration Token.
*   Initializing the SDK with OAuth credentials.
*   Making an example API call (`searchContactsAdvanced`).
*   Configuring session storage (MongoDB example).
*   Processing webhooks and verifying signatures.
*   Links to additional SDK resources and examples on GitHub and Packagist are provided.