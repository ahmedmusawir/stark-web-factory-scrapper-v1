Here is a structured summary of the provided API documentation:

### 1. ENDPOINT NAME:
Delete Note

### 2. CATEGORY:
Calendars / Appointment Notes

### 3. HTTP METHOD:
DELETE

### 4. ENDPOINT URL:
`/calendars/appointments/:appointmentId/notes/:noteId`

### 5. FULL URL:
`https://services.leadconnectorhq.com/calendars/appointments/:appointmentId/notes/:noteId`

### 6. REQUIRED PARAMETERS:
*   **Header Parameters:**
    *   `Version` (string): API Version. Possible values: `2021-04-15`
*   **Path Parameters:**
    *   `appointmentId` (string): Appointment ID
    *   `noteId` (string): Note ID

### 7. OPTIONAL PARAMETERS:
Not specified in documentation

### 8. AUTHENTICATION:
*   **Scope(s):** `calendars/events.write`
*   **Auth Method(s):** `OAuth Access Token`, `Private Integration Token`
*   **Token Type(s):** `Sub-Account Token`
*   **Authorization Header:** `Bearer <TOKEN>`

### 9. RESPONSE FORMAT:

*   **Successful Response (200 OK):**
    *   **Content Type:** `application/json`
    *   **Schema:**
        ```json
        {
          "success": boolean
        }
        ```
    *   **Example:**
        ```json
        {"success": true}
        ```
*   **Error Responses:**
    *   **400 Bad Request:**
        ```json
        {"statusCode": 400, "message": "Bad Request"}
        ```
    *   **401 Unauthorized:**
        ```json
        {"statusCode": 401, "message": "Invalid token: access token is invalid", "error": "Unauthorized"}
        ```

### 10. PURPOSE:
This endpoint allows users to delete a specific note associated with a particular appointment in the system.

### 11. TAGS:
Calendars, Appointments, Notes, Delete, Remove, GHL, HighLevel, API, Events, Write

### 12. CODE EXAMPLES:
Code examples are available for the following languages/tools (with `curl` being explicitly shown):
*   curl
*   nodejs
*   python
*   php
*   java
*   go
*   ruby
*   powershell