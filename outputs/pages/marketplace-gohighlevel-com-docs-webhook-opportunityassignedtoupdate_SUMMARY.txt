Here's a structured summary of the API documentation for the `OpportunityAssignedToUpdate` webhook:

### 1. ENDPOINT NAME
OpportunityAssignedToUpdate

### 2. CATEGORY
Webhook (related to Opportunities)

### 3. HTTP METHOD
Not applicable (this is a webhook event notification from HighLevel to a subscribed URL).
For a server *receiving* this webhook, the expected method would typically be POST.

### 4. ENDPOINT URL
Not applicable (subscribers configure their own URL to receive this webhook event).

### 5. FULL URL
Not applicable (subscribers configure their own URL to receive this webhook event).

### 6. REQUIRED PARAMETERS
*Note*: The provided JSON schema does not include a `required` array. However, for webhook event payloads, all properties listed in the schema are typically expected to be present in the event data. These are the fields included in the `OpportunityAssignedToUpdate` webhook payload:
- **type** (string): The type of event. Example: `OpportunityAssignedToUpdate`
- **locationId** (string): The ID of the location (sub-account) where the opportunity exists.
- **id** (string): The unique identifier of the opportunity.
- **assignedTo** (string): The ID of the user to whom the opportunity is assigned.
- **contactId** (string): The ID of the contact associated with the opportunity.
- **monetaryValue** (number): The monetary value of the opportunity.
- **name** (string): The name or title of the opportunity.
- **pipelineId** (string): The ID of the pipeline to which the opportunity belongs.
- **pipelineStageId** (string): The ID of the stage within the pipeline where the opportunity is currently located.
- **source** (string): The source from which the opportunity originated.
- **status** (string): The current status of the opportunity (e.g., "open", "won", "lost").
- **dateAdded** (string): The date and time when the opportunity was added, in ISO 8601 format (e.g., `2021-11-26T12:41:02.193Z`).

### 7. OPTIONAL PARAMETERS
Not applicable (all fields expected in the webhook payload are listed above).

### 8. AUTHENTICATION
Not specified in documentation for this specific webhook event. General webhook integration guides usually cover security mechanisms such as shared secrets for signature verification.

### 9. RESPONSE FORMAT
The provided documentation describes the JSON payload *sent by HighLevel* when the `OpportunityAssignedToUpdate` event occurs.
A server receiving this webhook should typically respond with a `2xx` HTTP status code (e.g., `200 OK`) to acknowledge successful receipt and processing of the event.

### 10. PURPOSE
This webhook is called whenever an opportunity's `AssignedTo` field is updated within HighLevel.

### 11. TAGS
Webhook Events, Opportunity, Update, AssignedTo, Notification, CRM.