Here's a structured summary of the API documentation:

### 1. ENDPOINT NAME:
Get Record By Id

### 2. CATEGORY:
Objects

### 3. HTTP METHOD:
GET

### 4. ENDPOINT URL:
`/objects/:schemaKey/records/:id`

### 5. FULL URL:
`https://services.leadconnectorhq.com/objects/:schemaKey/records/:id`

### 6. REQUIRED PARAMETERS:
*   **Version** (string): API Version. Possible values: `2021-07-28` (Header Parameter)
*   **schemaKey** (string): The key of the Custom Object / Standard Object Schema. For custom objects, the key must include the “custom_objects.” prefix, while standard objects use their respective object keys. This information is available on the Custom Objects Details page under Settings. Example: `custom_objects.pet` or `business.email`. (Path Parameter)
*   **id** (string): ID of the record to be updated. Available on the Record details page under the 3 dots or in the URL. Example: `632c34b4c9b7da3358ac9891`. (Path Parameter)

### 7. OPTIONAL PARAMETERS:
Not specified in documentation

### 8. AUTHENTICATION:
*   **Auth Method(s)**: `OAuth Access Token`, `Private Integration Token`
*   **Token Type(s)**: `Sub-Account Token`
*   **Scheme**: Bearer (JWT)
*   **Location**: `Authorization` header

### 9. RESPONSE FORMAT:
**Status Code:** `200 OK`
**Content Type:** `application/json`
**Structure:**
```json
{
  "record": {
    "id": "string",
    "owner": [
      "string"
    ],
    "followers": [
      "string"
    ],
    "properties": {
      "customer_number": "number",
      "ticket_name": "string",
      "phone_number": "string",
      "money": {
        "currency": "string",
        "value": "number"
      },
      "type_of_ticket": "string",
      "section_of_app": [
        "string"
      ],
      "recieved_on": "string",
      "my_files": [
        {
          "url": "string"
        }
      ],
      "my_textbox_list.option_a": "string",
      "my_textbox_list.option_b": "string"
    },
    "dateAdded": "date-time",
    "dateUpdated": "date-time"
  }
}
```
**Error Responses:**
*   **400 Bad Request:** `{"statusCode":400,"message":"Bad Request"}`
*   **401 Unauthorized:** `{"statusCode":401,"message":"Invalid token: access token is invalid","error":"Unauthorized"}`

### 10. PURPOSE:
This endpoint allows you to retrieve a specific record, belonging to either a Standard Object (like business) or a Custom Object, by its unique ID.

### 11. TAGS:
Objects, Records, Get, Retrieve, Read, Custom Objects, Standard Objects, Record by ID, HighLevel API

### CODE EXAMPLES:
Code examples are available for: `curl`, `nodejs`, `python`, `php`, `java`, `go`, `ruby`, `powershell`.