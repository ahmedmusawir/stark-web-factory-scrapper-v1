```
## STRUCTURED SUMMARY FOR API ENDPOINT: Record Update

### 1. ENDPOINT NAME
Record Update

### 2. CATEGORY
Webhook

### 3. HTTP METHOD
POST (as indicated in "Additional Notes" for the webhook listener)

### 4. ENDPOINT URL
Not applicable. This is a webhook event sent by HighLevel *to* a URL configured by the user. There is no specific endpoint path *on HighLevel's side* that a user would call to trigger this event.

### 5. FULL URL
Not applicable. The URL provided in the prompt is for the documentation page, not the webhook delivery URL. The webhook will be sent to a URL provided by the user during configuration.

### 6. REQUIRED PARAMETERS (Webhook Payload Fields)
*   **type** (string): Indicates the type of record created.
*   **locationId** (string): Identifies the location associated with the created record.
*   **owners** (array of strings): Represents the unique identifiers of users who own the record.
*   **followers** (array of strings): List of users who are following the record for updates.
*   **properties** (array of objects): Contains key-value pairs representing additional details about the record.
    *   **key** (string): The property name.
    *   **valueString** (string): The corresponding value as a string.
*   **id** (string): Unique identifier for the created record.
*   **timestamp** (string, `date-time` format): Represents the date and time when the record was created.

### 7. OPTIONAL PARAMETERS
Not specified in documentation. All fields listed in the schema are expected components of the webhook payload.

### 8. AUTHENTICATION
Not specified in documentation. (Webhooks commonly use signature verification or secret keys for security, but details are not provided here.)

### 9. RESPONSE FORMAT
The webhook *sends* a JSON payload to the configured listener.
**Status Code**: Not specified for the response *from* the webhook listener back to HighLevel, but typically a `200 OK` is expected for successful receipt.
**Payload Structure (JSON)**:
```json
{
  "type": "string",
  "locationId": "string",
  "owners": [
    "string"
  ],
  "followers": [
    "string"
  ],
  "properties": [
    {
      "key": "string",
      "valueString": "string"
    }
  ],
  "id": "string",
  "timestamp": "string"
}
```
**Example Response**:
```json
{
  "id": "679b8f9bde6a0c356a0311b3",
  "locationId": "eHy2cOSZxMQzQ6Yyvl8P",
  "timestamp": "2025-02-10T08:26:05.961Z",
  "owners": [
    "60d5ec49f72b2a001f5f9d91"
  ],
  "followers": [
    "60d5ec49f72b2a001f5f9d93",
    "60d5ec49f72b2a001f5f9d94"
  ],
  "properties": [
    {
      "key": "pet_name",
      "valueString": "buddy"
    }
  ]
}
```

### 10. PURPOSE
This webhook is triggered by HighLevel and sent to a user's listener when a record or business is updated within the HighLevel system.

### 11. TAGS
Webhook Response, Webhook, Record, Update, Business, Event
```