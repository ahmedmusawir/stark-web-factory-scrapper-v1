```
## STRUCTURED SUMMARY: HighLevel API - RecordDelete Webhook

### 1. ENDPOINT NAME:
Delete Record

### 2. CATEGORY:
Webhook

### 3. HTTP METHOD:
Not applicable. This document describes the payload of a webhook event sent *by* HighLevel, not an API endpoint to be invoked via HTTP methods.

### 4. ENDPOINT URL:
Not applicable. This document describes the payload of a webhook event sent *by* HighLevel, not a callable API endpoint.

### 5. FULL URL:
Not applicable. This document describes the payload of a webhook event sent *by* HighLevel, not a callable API endpoint.

### 6. REQUIRED PARAMETERS (Webhook Payload Fields):
These are the fields included in the JSON payload of the `RecordDelete` webhook event. All fields listed in the schema appear to be required as per the documentation and example.
*   `type` (string): The type of event.
*   `locationId` (string): Unique identifier for the location associated with the deleted record.
*   `owners` (array of strings): List of user IDs that were assigned as owners of the deleted record.
*   `followers` (array of strings): List of user IDs that were following the deleted record.
*   `properties` (array of objects): Key-value pairs containing additional metadata about the record.
    *   Inner object:
        *   `key` (string): The key of the property.
        *   `valueString` (string): The string value of the property.
*   `id` (string): Unique identifier of the deleted record.
*   `timestamp` (string (ISO 8601 format)): The timestamp when the deletion event occurred.

### 7. OPTIONAL PARAMETERS:
Not specified in documentation. All fields defined in the schema and explanation are presented as standard components of the webhook payload.

### 8. AUTHENTICATION:
Not specified in this documentation. Webhooks typically use mechanisms like shared secrets or signature verification for security, which are usually covered in a general "Webhook Integration Guide" rather than per-webhook documentation.

### 9. RESPONSE FORMAT:
Not applicable. This describes a webhook event *sent by* HighLevel. The receiving system is expected to acknowledge receipt, typically with a 2xx HTTP status code.

### 10. PURPOSE:
The `Delete Record` webhook is triggered whenever a record or business (company) is deleted from the system, providing details about the deleted entity.

### 11. TAGS:
*   Webhook Events
*   Record
*   Delete
*   Company
*   Business
*   Event
*   Notification

### AVAILABILITY OF CODE EXAMPLES:
No code examples (e.g., Python, Node.js, cURL) are present in this specific documentation page.
```