Here is a structured summary of the `OpportunityCreate` webhook documentation:

### 1. ENDPOINT NAME:
OpportunityCreate Webhook

### 2. CATEGORY:
Webhook (Specifically, Opportunity-related webhooks)

### 3. HTTP METHOD:
N/A (This documentation describes the payload of a webhook *sent by* the HighLevel system, not an API endpoint to call *to* the system.)

### 4. ENDPOINT URL:
N/A (This describes the webhook payload structure, not a callable API endpoint.)

### 5. FULL URL:
N/A (This is the documentation URL for the webhook event, not the URL where the webhook is sent or an API endpoint.)

### 6. REQUIRED PARAMETERS (Webhook Payload Fields):
*Note: For a webhook, these are the fields that will be present in the payload received by your listener when an opportunity is created.*
*   `type` (string): The type of event (e.g., "OpportunityCreate").
*   `locationId` (string): The ID of the location where the opportunity was created.
*   `id` (string): The unique ID of the opportunity.
*   `assignedTo` (string): The ID of the user or team assigned to the opportunity.
*   `contactId` (string): The ID of the contact associated with the opportunity.
*   `monetaryValue` (number): The monetary value of the opportunity.
*   `name` (string): The name of the opportunity.
*   `pipelineId` (string): The ID of the pipeline the opportunity belongs to.
*   `pipelineStageId` (string): The ID of the stage within the pipeline the opportunity is currently in. (Note: This property is listed twice in the provided schema, but refers to the same concept.)
*   `source` (string): The source from which the opportunity originated.
*   `status` (string): The current status of the opportunity (e.g., "open").
*   `dateAdded` (string): The timestamp when the opportunity was added, in ISO 8601 format.

### 7. OPTIONAL PARAMETERS:
Not specified in documentation. All parameters listed in the schema are typically included in the webhook payload.

### 8. AUTHENTICATION:
Not specified in documentation. (Webhooks typically use a shared secret or signature for verification, which would need to be configured on the receiving end).

### 9. RESPONSE FORMAT:
N/A (This describes the JSON payload *received* by a webhook listener. There is no traditional API response from the HighLevel system to an external client for this event.)

### 10. PURPOSE:
This webhook is triggered and sends a payload to a registered listener whenever a new opportunity is created within the HighLevel system.

### 11. TAGS:
Webhook Events, Opportunity, Create, CRM, GoHighLevel