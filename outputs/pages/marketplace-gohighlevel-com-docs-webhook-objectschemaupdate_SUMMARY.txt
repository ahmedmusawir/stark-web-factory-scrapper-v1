Here's a structured summary of the `ObjectSchemaUpdate` webhook documentation:

### 1. ENDPOINT NAME
ObjectSchemaUpdate (Update Custom Object)

### 2. CATEGORY
Webhook

### 3. HTTP METHOD
POST (expected by the webhook listener)

### 4. ENDPOINT URL
Not applicable (This documentation describes the payload of a webhook sent *from* HighLevel, not an API endpoint to call *on* HighLevel. The URL is configured by the user on the HighLevel platform for their listener.)

### 5. FULL URL
Not applicable (See ENDPOINT URL)

### 6. REQUIRED PARAMETERS
*   **labels** (object): An object that defines the human-readable names associated with the custom object.
    *   **singular** (string): The name of the object in singular form (e.g., `"pet"`).
    *   **plural** (string): The name of the object in plural form (e.g., `"pets"`).
*   **primaryDisplayProperty** (string): The key property used to display the custom object.
*   **key** (string): Unique identifier for the custom object type.
*   **locationId** (string): Identifies the location associated with the custom object.

### 7. OPTIONAL PARAMETERS
*   **description** (string): A brief explanation of the custom object.
*   **searchableProperties** (array): List of properties that are indexed for search.
*   **createdBy** (object): Metadata about the user who created the object.
*   **updatedBy** (object): Metadata about the user who last updated the object.
*   **timestamp** (string, `date-time` format): The date and time when the object was created.
*   **objectType** (string, enum: `"USER_DEFINED"`, default: `"USER_DEFINED"`): Specifies the type of object, currently supports only `USER_DEFINED`.

### 8. AUTHENTICATION
Not specified in documentation for receiving the webhook. Webhooks commonly use a signature in the request headers (not detailed here) for verification purposes by the recipient.

### 9. RESPONSE FORMAT
Not specified for the recipient's response, but typically an HTTP 2xx status code (e.g., 200 OK) is expected by the sender for acknowledgment that the webhook was received successfully.

### 10. PURPOSE
This webhook is triggered whenever a custom object is updated, allowing integrated systems to listen for these changes and take appropriate actions.

### 11. TAGS
Webhook Events

### Availability of Code Examples
No code examples (e.g., Python, Node, cURL) are present in the documentation.