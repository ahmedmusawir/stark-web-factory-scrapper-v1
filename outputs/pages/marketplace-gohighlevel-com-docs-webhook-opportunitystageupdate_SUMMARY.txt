Here's a structured summary of the OpportunityStageUpdate webhook documentation:

### 1. ENDPOINT NAME:
OpportunityStageUpdate

### 2. CATEGORY:
Webhook (specifically, Opportunity related)

### 3. HTTP METHOD:
N/A (This is a webhook event, not an API endpoint to be called. It represents data *sent* by the HighLevel platform to a configured URL.)

### 4. ENDPOINT URL:
N/A (As a webhook, HighLevel *sends* a POST request to a URL configured by the user/application, not an endpoint to be called *on* HighLevel's side.)

### 5. FULL URL:
N/A (See above)

### 6. REQUIRED PARAMETERS:
Not specified in documentation. All fields listed in the schema are expected to be part of the webhook payload.

### 7. OPTIONAL PARAMETERS:
Not specified in documentation. All fields listed in the schema are expected to be part of the webhook payload.

**Payload Fields (All fields present in the webhook payload):**
*   **type** (string): The type of the webhook event, which is "OpportunityStageUpdate".
*   **locationId** (string): The unique identifier of the location (sub-account) where the opportunity was updated.
*   **id** (string): The unique identifier of the opportunity.
*   **assignedTo** (string): The unique identifier of the user to whom the opportunity is assigned.
*   **contactId** (string): The unique identifier of the contact associated with the opportunity.
*   **monetaryValue** (number): The monetary value of the opportunity.
*   **name** (string): The name of the opportunity.
*   **pipelineId** (string): The unique identifier of the pipeline the opportunity belongs to.
*   **pipelineStageId** (string): The unique identifier of the stage within the pipeline where the opportunity currently resides (after the update).
*   **source** (string): The source from which the opportunity originated.
*   **status** (string): The current status of the opportunity (e.g., "open", "won", "lost").
*   **dateAdded** (string): The date and time when the opportunity was initially added, in ISO 8601 format (e.g., "2021-11-26T12:41:02.193Z").

### 8. AUTHENTICATION:
Not specified in this specific webhook documentation. Webhooks typically rely on shared secrets or signature verification for security, but no details are provided here.

### 9. RESPONSE FORMAT:
A successful response to the webhook payload (from the receiving server) should typically be a 2xx HTTP status code to acknowledge receipt. The webhook *payload* itself is a JSON object with the following structure:

**Status Code:** N/A (The webhook *sends* a payload; it does not receive a response in this context.)

**Payload Structure:**
```json
{
  "type": "string",
  "locationId": "string",
  "id": "string",
  "assignedTo": "string",
  "contactId": "string",
  "monetaryValue": "number",
  "name": "string",
  "pipelineId": "string",
  "pipelineStageId": "string",
  "source": "string",
  "status": "string",
  "dateAdded": "string"
}
```

**Example Payload:**
```json
{
  "type": "OpportunityStageUpdate",
  "locationId": "ve9EPM428h8vShlRW1KT",
  "id": "wWhVuzqpRuOA1ZVWi4FC",
  "assignedTo": "bNl8QNGXhIQJLv8eeASQ",
  "contactId": "cJAWDskpkJHbRbhAT7bs",
  "monetaryValue": 40,
  "name": "Loram ipsu",
  "pipelineId": "VDm7RPYC2GLUvdpKmBfC",
  "pipelineStageId": "e93ba61a-53b3-45e7-985a-c7732dbcdb69",
  "source": "Loram ipsu",
  "status": "open",
  "dateAdded": "2021-11-26T12:41:02.193Z"
}
```

### 10. PURPOSE:
This webhook is called whenever an opportunity's stage field is updated within the HighLevel platform.

### 11. TAGS:
Webhook Events, Opportunity, Stage Update, CRM, Sales Pipeline, Notification