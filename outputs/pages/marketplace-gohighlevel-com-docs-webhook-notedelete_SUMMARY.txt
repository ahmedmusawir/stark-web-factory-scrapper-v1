```
## STRUCTURED SUMMARY FOR NoteDelete Webhook Event

### 1. ENDPOINT NAME:
NoteDelete

### 2. CATEGORY:
Webhook

### 3. HTTP METHOD:
Not applicable (This describes a webhook event sent by the system, not an API endpoint called by a user).

### 4. ENDPOINT URL:
Not applicable (This is a webhook event payload description, not a callable API endpoint).

### 5. FULL URL:
Not applicable (This is a webhook event payload description, not a callable API endpoint).

### 6. REQUIRED PARAMETERS (Webhook Payload Fields):
*   `type` (string): The type of the webhook event.
*   `locationId` (string): The unique identifier of the location where the note was deleted.
*   `id` (string): The unique identifier of the deleted note.
*   `body` (string): The content or body of the deleted note.
*   `contactId` (string): The unique identifier of the contact associated with the deleted note.
*   `dateAdded` (string): The date and time when the note was originally added, in ISO 8601 format.

### 7. OPTIONAL PARAMETERS:
Not specified in documentation

### 8. AUTHENTICATION:
Not specified in documentation for this specific webhook event. General API authorization (OAuth 2.0) is mentioned elsewhere in the broader documentation. For webhooks, typically a shared secret or signature verification is used, but not detailed here.

### 9. RESPONSE FORMAT:
Not applicable (This documentation describes the payload of an *outgoing* webhook event, not a response to a user's API call. A webhook receiver typically responds with a 200 OK status to acknowledge receipt).

### 10. PURPOSE:
This webhook event is called whenever a note is deleted within the HighLevel system.

### 11. TAGS:
Webhook Events, Note, Delete, Contact, Location, Event

### Code Examples:
Not specified in documentation (only JSON schema and example payload are provided).
```