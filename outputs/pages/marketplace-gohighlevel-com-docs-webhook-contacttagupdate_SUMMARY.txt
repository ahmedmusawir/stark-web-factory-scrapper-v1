Here's a structured summary of the ContactTagUpdate webhook documentation:

---

### 1. ENDPOINT NAME:
ContactTagUpdate

### 2. CATEGORY:
Webhook / Contacts

### 3. HTTP METHOD:
Webhook (outgoing POST payload description). This documentation describes the payload sent *by* HighLevel when the event occurs, not an API endpoint to be called *by* an external application.

### 4. ENDPOINT URL:
Not applicable (describes webhook payload structure, not an API endpoint path to call).

### 5. FULL URL:
`https://marketplace.gohighlevel.com/docs/webhook/ContactTagUpdate/index.html`

### 6. REQUIRED PARAMETERS:
Not explicitly specified for the webhook payload fields. All fields listed in the "Schema" are potential fields in the webhook payload. Typically, `type`, `locationId`, and `id` are always present.

### 7. OPTIONAL PARAMETERS:
The documentation does not explicitly differentiate between required and optional fields for the webhook payload. All fields listed in the schema are present in the payload based on the contact's data at the time of the update.

**Webhook Payload Fields (as per Schema):**
*   `type` (string): The type of the webhook event.
*   `locationId` (string): The unique identifier for the location (sub-account).
*   `id` (string): The unique identifier for the contact.
*   `address1` (string): The first line of the contact's address.
*   `city` (string): The city of the contact's address.
*   `companyName` (string): The name of the company the contact is associated with.
*   `country` (string): The country of the contact's address.
*   `source` (string): The source from which the contact was added (e.g., "xyz form").
*   `dateAdded` (string): The date and time the contact was added (ISO 8601 format).
*   `dateOfBirth` (string): The contact's date of birth (ISO 8601 format).
*   `dnd` (boolean): Boolean indicating if the contact has Do Not Disturb enabled.
*   `email` (string): The contact's email address.
*   `name` (string): The full name of the contact.
*   `firstName` (string): The first name of the contact.
*   `lastName` (string): The last name of the contact.
*   `phone` (string): The contact's phone number.
*   `postalCode` (string): The postal code of the contact's address.
*   `state` (string): The state of the contact's address.
*   `tags` (array): An array of strings representing the tags associated with the contact.
*   `website` (string): The contact's website URL.
*   `attachments` (array): An array of attachments (details of items not specified in schema).
*   `assignedTo` (string): The ID of the user assigned to the contact.
*   `customFields` (array of objects): An array of custom fields associated with the contact.
    *   `id` (string): The ID of the custom field.
    *   `value` (string, number, array, object): The value of the custom field, which can be of various types.

### 8. AUTHENTICATION:
Not specified for the webhook payload itself. HighLevel sends the webhook to a pre-configured URL. Receivers of the webhook would typically implement their own authentication/verification mechanism (e.g., signature verification using a shared secret) to ensure the request originated from HighLevel.

### 9. RESPONSE FORMAT:
N/A. This documentation describes the payload that HighLevel *sends*. When an external system receives this webhook, it is expected to return an HTTP 2xx status code (e.g., 200 OK) to acknowledge successful receipt and processing.

### 10. PURPOSE:
This webhook is called whenever a contact's tag field is updated within the HighLevel system.

### 11. TAGS:
Webhook Events, Contact, Tag Update, ContactTagUpdate, Contact Management.

---
**Availability of Code Examples:**
No code examples (Python, Node, cURL, etc.) are present in the provided documentation for interacting with this webhook.