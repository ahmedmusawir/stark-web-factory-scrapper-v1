Here's a structured summary of the `ContactDelete` webhook documentation:

### 1. ENDPOINT NAME:
ContactDelete

### 2. CATEGORY:
Webhook, Contacts

### 3. HTTP METHOD:
Not applicable (This is a webhook event sent by HighLevel, not an API endpoint to be called with an HTTP method.)

### 4. ENDPOINT URL:
Not applicable (This is a webhook event; it's a notification *from* HighLevel, not an endpoint to send requests *to*.)

### 5. FULL URL:
Not applicable (This is a webhook event; it's a notification *from* HighLevel.)

### 6. REQUIRED PARAMETERS (Webhook Payload Fields):
These are the fields that are part of the JSON payload sent by the `ContactDelete` webhook. The documentation does not explicitly distinguish between required and optional fields within the payload; all fields defined in the schema are typically included, though some might have null or empty values depending on the specific contact data.

*   **type** (string): The type of the webhook event, which is "ContactDelete".
*   **locationId** (string): The unique identifier of the location (sub-account) where the contact was deleted.
*   **id** (string): The unique identifier of the deleted contact.
*   **address1** (string): The primary address line of the deleted contact.
*   **city** (string): The city of the deleted contact's address.
*   **companyName** (string): The company name associated with the deleted contact.
*   **country** (string): The country of the deleted contact's address.
*   **source** (string): The source from which the contact was added (e.g., "xyz form").
*   **dateAdded** (string): The date and time the contact was originally added, in ISO 8601 format.
*   **dateOfBirth** (string): The date of birth of the deleted contact, in ISO 8601 format.
*   **dnd** (boolean): A boolean indicating if the contact is set to 'Do Not Disturb'.
*   **email** (string): The email address of the deleted contact.
*   **name** (string): The full name of the deleted contact.
*   **firstName** (string): The first name of the deleted contact.
*   **lastName** (string): The last name of the deleted contact.
*   **phone** (string): The phone number of the deleted contact.
*   **postalCode** (string): The postal code of the deleted contact's address.
*   **state** (string): The state/province of the deleted contact's address.
*   **tags** (array of strings): An array of tags associated with the deleted contact.
*   **website** (string): The website URL associated with the deleted contact.
*   **attachments** (array): An array of attachment objects associated with the deleted contact. (Specific structure of items not detailed, but type is array).
*   **assignedTo** (string): The unique identifier of the user to whom the contact was assigned.
*   **customFields** (array of objects): An array of custom field objects.
    *   **id** (string): The ID of the custom field.
    *   **value** (string, number, array, or object): The value of the custom field.

### 7. OPTIONAL PARAMETERS:
Not specified in documentation. All fields listed in the schema are part of the potential webhook payload structure.

### 8. AUTHENTICATION:
Not specified in documentation for receiving webhooks. (Typically, for receiving webhooks, authentication involves verifying the sender using a shared secret or signature, but this document does not detail that process).

### 9. RESPONSE FORMAT:
Not applicable (This is a webhook event sent *by* HighLevel. The recipient of the webhook typically responds with an HTTP 2xx status code (e.g., 200 OK) to acknowledge successful receipt, but this is not a response *from* HighLevel).

### 10. PURPOSE:
This webhook is called whenever a contact is deleted within HighLevel.

### 11. TAGS:
Webhook Events, Contact, Delete, CRM, Notification

### 12. Code Examples:
No code examples (e.g., cURL, Python, Node.js) are present in this specific documentation section.