Here is a structured summary of the API documentation:

### 1. ENDPOINT NAME:
Update invoice last visited at

### 2. CATEGORY:
Invoice

### 3. HTTP METHOD:
PATCH

### 4. ENDPOINT URL:
`/invoices/stats/last-visited-at`

### 5. FULL URL:
`https://services.leadconnectorhq.com/invoices/stats/last-visited-at`

### 6. REQUIRED PARAMETERS:
*   **Header Parameters:**
    *   Version (string): API Version. Possible values: `2021-07-28`
*   **Body Parameters (application/json):**
    *   invoiceId (string): Invoice Id. Example: `6578278e879ad2646715ba9c`

### 7. OPTIONAL PARAMETERS:
Not specified in documentation

### 8. AUTHENTICATION:
*   **Auth Method(s):** `OAuth Access Token`, `Private Integration Token`
*   **Token Type(s):** `Sub-Account Token`, `Agency Token`
*   **Scheme:** Bearer (JWT format)
*   **In:** Header (Authorization header)

### 9. RESPONSE FORMAT:
*   **200 OK:** Success (No specific schema or example provided for a 200 OK response in the documentation).
*   **400 Bad Request (application/json):**
    *   `statusCode` (number): e.g., `400`
    *   `message` (string): e.g., `Bad Request`
    *   Example: `{"statusCode":400,"message":"Bad Request"}`
*   **401 Unauthorized (application/json):**
    *   `statusCode` (number): e.g., `401`
    *   `message` (string): e.g., `Invalid token: access token is invalid`
    *   `error` (string): e.g., `Unauthorized`
    *   Example: `{"statusCode":401,"message":"Invalid token: access token is invalid","error":"Unauthorized"}`

### 10. PURPOSE:
This API endpoint updates the `last visited at` timestamp for a specific invoice identified by its ID.

### 11. TAGS:
Invoice, Update, Last Visited At, Statistics, Financial, API, GoHighLevel, Patch

**Code Examples Availability:** `curl`, `nodejs`, `python`, `php`, `java`, `go`, `ruby`, `powershell` (cURL example is provided).