Here's a structured summary of the API documentation:

### 1. ENDPOINT NAME
Update invoice late fees configuration

### 2. CATEGORY
Invoice

### 3. HTTP METHOD
PATCH

### 4. ENDPOINT URL
`/invoices/:invoiceId/late-fees-configuration`

### 5. FULL URL
`https://services.leadconnectorhq.com/invoices/:invoiceId/late-fees-configuration`

### 6. REQUIRED PARAMETERS
*   **Header Parameters:**
    *   `Version` (string): API Version. Possible values: `2021-07-28`
*   **Path Parameters:**
    *   `invoiceId` (string): Invoice Id. Example: `6578278e879ad2646715ba9c`
*   **Body Parameters (application/json):**
    *   `altId` (string): location Id / company Id based on altType. Example: `6578278e879ad2646715ba9c`
    *   `altType` (string): Alt Type. Possible values: `location`. Example: `location`
    *   `lateFeesConfiguration` (object): late fees configuration
        *   `enable` (boolean): Enable late fees. Example: `true`
        *   `value` (number): Late Fees Value. Example: `10`
        *   `type` (string): Late Fees Type. Possible values: `fixed`, `percentage`. Example: `fixed`
        *   `frequency` (object): Late Fees Frequency
            *   `intervalCount` (number): Late fees interval count. Example: `10`
            *   `interval` (string): Late fees interval. Possible values: `minute`, `hour`, `day`, `week`, `month`, `one_time`. Example: `day`

### 7. OPTIONAL PARAMETERS
*   **Body Parameters (within `lateFeesConfiguration` object):**
    *   `grace` (object): Late Fees Grace.
        *   `intervalCount` (number): Late fees grace interval count. (Required if `grace` object is provided). Example: `10`
        *   `interval` (string): Late fees grace interval. Possible values: `day`. (Required if `grace` object is provided). Example: `day`
    *   `maxLateFees` (object): Max late fees payable.
        *   `type` (string): (Required if `maxLateFees` object is provided). Possible values: `fixed`. Example: `fixed`
        *   `value` (number): Max late fees to pay. (Required if `maxLateFees` object is provided). Example: `10`

### 8. AUTHENTICATION
*   **Auth Method(s):** `OAuth Access Token`, `Private Integration Token`
*   **Token Type(s):** `Sub-Account Token`, `Agency Token`
*   **Header:** `Authorization: Bearer <TOKEN>` (Bearer JWT format)

### 9. RESPONSE FORMAT

**200 OK - Successful response (application/json)**
Returns an Invoice object with the updated late fees configuration.
```json
{
  "_id": "6578278e879ad2646715ba9c",
  "status": "draft",
  "liveMode": false,
  "amountPaid": 0,
  "altId": "6578278e879ad2646715ba9c",
  "altType": "location",
  "name": "New Invoice",
  "businessDetails": { /* ... */ },
  "invoiceNumber": "19",
  "currency": "USD",
  "contactDetails": { /* ... */ },
  "issueDate": "2023-01-01",
  "dueDate": "2023-01-01",
  "discount": { "type": "percentage", "value": 0 },
  "invoiceItems": [ /* ... */ ],
  "total": 999,
  "title": "INVOICE",
  "amountDue": 999,
  "createdAt": "2023-12-12T09:27:42.355Z",
  "updatedAt": "2023-12-12T09:27:42.355Z",
  "automaticTaxesEnabled": true,
  "automaticTaxesCalculated": true,
  "paymentSchedule": {}
  // lateFeesConfiguration object would be included here after update
}
```

**400 Bad Request (application/json)**
```json
{"statusCode":400,"message":"Bad Request"}
```

**401 Unauthorized (application/json)**
```json
{"statusCode":401,"message":"Invalid token: access token is invalid","error":"Unauthorized"}
```

**422 Unprocessable Entity (application/json)**
```json
{"statusCode":422,"message":["Unprocessable Entity"],"error":"Unprocessable Entity"}
```

### 10. PURPOSE
This API allows updating the late fees configuration for a specific invoice identified by its ID.

### 11. TAGS
Invoice, Late Fees, Configuration, Update, GHL, LeadConnector

### 12. CODE EXAMPLES
Code examples are available for: `curl`, `nodejs`, `python`, `php`, `java`, `go`, `ruby`, `powershell`.