# BIM001 QA retest — Architect-directed repair

## 1. New candidate identity

**EVIDENCE:** First actions were the requested read-only identity checks:

```text
$ git branch --show-current
qa/web-factory-p1-bim001
$ git rev-parse HEAD
eee039a2c06cc1aeaa3e9dfb52e17db9d3ddb20a
$ git log -1 --oneline
eee039a 6sep2026 - repair 1 for bim001 done by claudy
$ git status --short
?? agent_docs/ACTION/web-factory-p1-bim001/QA/INPUTS/
```

**New pinned candidate:** `eee039a2c06cc1aeaa3e9dfb52e17db9d3ddb20a`. Previous independently tested candidate: `657e25e9191e5d91a822a49301c0260bde8f1bd4`. Main baseline: `57a0f597b0ae6c15cfc9a43d5bebf367a6ebb2f0`.

Only supplied QA-lane inputs were untracked at intake. No unexpected tracked working-tree drift. The candidate contains the prior QA artifacts as committed history; fresh results use **`evidence/retest_eee039a/`** and do not overwrite old evidence. [Integrity proof](evidence/retest_eee039a/integrity.json) confirms pinned HEAD, empty tracked diff, and identical actual repository output file hashes/sizes/mtimes before and after retest. Final status contains only authorized QA additions.

**EVIDENCE — scope:** This was a bounded repair retest, not a Stage 2 restart. Existing source/tests/contracts were not edited by Cody. No package installation, cache deletion, network crawl, or git mutation occurred. Probe browser calls are stubbed; the existing QA probe rejects network connection/DNS attempts and recorded zero attempts. All generated fixtures, outputs, logs, JUnit and temporary files remain in the QA lane. Tony retains git authority; SOL owns live authorization and Gate Q. This report is not certification or Gate Q.

## 2. Repair-diff scope

**CLAIM source read completely:** [ENGINEERING_REPAIR_BIM001_2026-09-06_1528.md](ENGINEERING_REPAIR_BIM001_2026-09-06_1528.md). Its reported equivalence and passing tests were treated as claims, not adopted as independent results.

| Engineering repair claim | Independent result |
|---|---|
| input_hosts uses hostname, skips None, sorted unique | Source AST, direct edge inputs and newly persisted port manifest verified (§3) |
| FakeCrawler restored exactly to main | Entire class source and class-to-fixture bytes equal (§4) |
| Only BIM000 amendments a/b/c remain | Baseline definition comparison, normalized allowed changes, full test diff and unchanged assertions verified (§4) |
| Exception simulation moved to BIM001 subclass; AC-25 uses it | Source references and direct helper behavior verified; affected test passes |
| AC-32 extended test and 54-test regression green | Independently rerun; exact results below |
| Changelog corrected, no broader product scope | Diff and source verified (§5/11) |

**EVIDENCE:** `git diff 657e25e HEAD -- smart_crawler/crawler.py tests/test_crawler.py CHANGELOG.md` shows only the targeted repair. [Exact repair product diff](evidence/retest_eee039a/repair_product_diff.stdout.txt); [complete repair name-status list](evidence/retest_eee039a/repair_surface.stdout.txt). Outside the QA evidence lane, exactly these three files changed:

- `smart_crawler/crawler.py`: input_hosts docstring plus hostname/set/filter expression.
- `tests/test_crawler.py`: restore FakeCrawler; add BIM001 RaisingFakeCrawler; allow `_crawl` to select it; AC-25 selects it; extend AC-32 assertions/fixtures.
- `CHANGELOG.md`: remove extra helper behavior from BIM000 amendment (c), add bounded repair note.

The large full commit surface includes retained previous QA evidence and the Engineering repair report. It does not represent hundreds of new product changes. No frozen contract changes occurred between the old and new candidate ([surface_verified.json](evidence/retest_eee039a/surface_verified.json)).

## 3. AC-32 independent retest

**PASS. EVIDENCE — source:** `smart_crawler/crawler.py:315–317` now reads:

```python
def input_hosts(urls: Sequence[str]) -> list[str]:
    """Sorted unique hostnames of the input URLs (hostname, not netloc: no ports; None skipped)."""
    return sorted({h for h in (urlparse(u).hostname for u in urls) if h})
```

AST inspection confirms actual attribute access is `.hostname`, not `.netloc` (the latter occurs only in the explanatory docstring). Direct execution with port/plain duplicates, mixed-case hosts, a non-URL string and a relative path produces exactly:

```json
["a.example.invalid", "b.example.invalid", "example.invalid"]
```

The two values without hostnames are skipped; the result is sorted and unique. DNS host-case normalization is expected `.hostname` behavior under the supplied Architect instruction, not a regression. Exact inputs and actual/expected outputs: [source_verification.json](evidence/retest_eee039a/source_verification.json).

**EVIDENCE — independent persisted probe:** Reused the original QA-only `offline_probe.py` in a fresh subprocess/case directory with:

```text
https://example.invalid:8443/port
https://example.invalid/plain
```

Actual serialized/reloaded manifest `input_hosts` is **`["example.invalid"]`**; it contains no `example.invalid:8443`. Process exits **0**, two pages are captured, and network attempts are **0**. The actual `main/run/capture/close` path runs with browser results stubbed and output paths relocated only inside that subprocess.

[Persisted manifest](evidence/retest_eee039a/port_case/outputs/CyberizeGroup/runs/2026-09-06T09-47-56Z/manifest.json), [input fixture](evidence/retest_eee039a/port_case/input.json), [verification](evidence/retest_eee039a/port_verified.json), [command/output metadata](evidence/retest_eee039a/port_probe.command.json), [stdout](evidence/retest_eee039a/port_probe.stdout.txt).

The extended `test_ac32_manifest_identity_values` independently passed in the affected test run and both full suites. It also verifies input hosts are derived from all input URLs, including port-bearing URLs beyond the limit.

## 4. AC-71 independent retest

**PASS. EVIDENCE:** Compared `tests/test_crawler.py::FakeCrawler` against `git show main:tests/test_crawler.py` using both AST-delimited exact source and the complete raw class-to-next-fixture block. Both are **identical**. [Baseline class bytes](evidence/retest_eee039a/FakeCrawler_main.txt), [candidate class bytes](evidence/retest_eee039a/FakeCrawler_candidate.txt), [machine-readable proof](evidence/retest_eee039a/source_verification.json).

Every baseline named function/class was compared to main. Only these three definitions differ:

| Allowed category | Current source | Independently verified amendment |
|---|---|---|
| (a) | `tests/test_crawler.py::sandbox`, line 46 | RUN_ROOT redirected to tmp_path; removing only that statement yields baseline-equivalent AST |
| (b) | `test_empty_input_writes_truthful_summary_without_crawling`, line 159 | argv gains --project TestProj; removing only those arguments yields baseline-equivalent AST |
| (c) | `_result`, lines 17–28 | optional html=None; attribute absent by default, set only when supplied; original result fields/defaults retained |

**EVIDENCE:** `git diff main -- tests/` is fully captured in [main_test_diff.stdout.txt](evidence/retest_eee039a/main_test_diff.stdout.txt). No other BIM000 definition changed, and all **47 existing Assert statements** across all **14 baseline tests** preserve exact source text. The changed BIM001 AC-32 assertion is not a BIM000 assertion.

**EVIDENCE — exception seam:** `RaisingFakeCrawler` is defined only in `tests/test_crawler.py:399–407`, under the BIM001 chunk-3 infrastructure section. Its only named use is `test_ac25_failed_and_exception_no_html` at line 445, via `crawler_cls=RaisingFakeCrawler`. `_crawl` at `:410–414` accepts the optional class, defaulting to unchanged FakeCrawler.

Direct local execution proves baseline FakeCrawler returns an Exception object unchanged, while RaisingFakeCrawler raises the same Exception instance and preserves call tracking. Neither makes network calls. The source/reference and dynamic-helper proof is in `source_verification.json`; AC-25 passes in all three test runs. The formerly disallowed change to BIM000 FakeCrawler is removed, rather than reclassified as permitted.

## 5. AC-82 changelog retest

**PASS. EVIDENCE:** `CHANGELOG.md:23–26` lists only (a) sandbox redirection, (b) main argv project, and (c) optional html field. Amendment (c) no longer mentions FakeCrawler exception behavior. Line 34 records the hostname-versus-netloc repair and identifies RaisingFakeCrawler as **BIM001-only infrastructure, not a BIM000 amendment**.

The existing BIM001 section still includes --project, run layout, raw HTML, manifest, absences, stage log, named amendments, and no pin changes. The new note adds no discovery, dependency, browser API, or other product scope. Exact change is in [repair_product_diff.stdout.txt](evidence/retest_eee039a/repair_product_diff.stdout.txt); parsed amendment text is retained in `source_verification.json`.

## 6. Recorded errata / interpretation

**EVIDENCE:** No pre-existing append-only erratum file was present. Created [QA_ERRATA_BIM001_2026-09-06.md](QA_ERRATA_BIM001_2026-09-06.md), with AC, authoritative ruling source, prior QA evidence, disposition and explicit “no frozen AC text modified” for every entry.

**Ruling source:** Architect Fable / Director-approved adjudication package, explicitly supplied in this retest instruction §6. Unlike the earlier pending engineering approval references, the current instruction supplies the adjudications directly. The doctrine journal J-04/J-16 supplies the preserve-contract/record-erratum process; it is not claimed to contain these later BIM001-specific decisions.

| AC | Recorded ruling | Evidence match | Disposition |
|---|---|---|---|
| AC-20 | “present” means meaningful/non-empty HTML; empty attribute may be unsupported/no html in result; no zero-byte file required | Prior independent empty-HTML persisted probe exactly matches; capture behavior unchanged by repair; full relevant tests pass | PASS WITH NOTE |
| AC-35 / I-2 | Colliding md_file records pages/base-slug.md actually written; literal pages/slug.md applies to noncollisions | Prior independent 3-way collision artifacts match; markdown/capture code unchanged; AC-35 affected test passes again | PASS WITH NOTE |
| AC-90 | RECOVERY.md allowed as process documentation, no product-code implication | New full surface satisfies original list plus this file (§11) | PASS WITH NOTE |
| AC-92 / I-1 | No Playwright import/API usage; metadata-only version lookup explicitly allowed; distinguish source from caches | Fresh `.py`-only grep yields only crawler.py:285 metadata key/lookup; imports/API behavior unchanged; no caches deleted | PASS WITH NOTE |

Exact adjudicated source-grep command:

```text
grep -rn --include='*.py' 'litellm\|playwright\|google.generativeai\|genai' smart_crawler/ discover_site/
```

[Command metadata](evidence/retest_eee039a/source_grep.command.json), [stdout](evidence/retest_eee039a/source_grep.stdout.txt), [stderr](evidence/retest_eee039a/source_grep.stderr.txt). Exit 0 because the allowed metadata line matches; this is not represented as a literal zero-hit result. Prior raw recursive grep including binary-cache messages remains preserved in the offline evidence. The product repair changes only input_hosts, so the previously inspected absence of Playwright imports/API calls remains valid.

All four rulings are fully recorded and match evidence. None remains PASS-PENDING-ADJUDICATION. Frozen contract text and historical reports were not rewritten.

## 7. Targeted affected test result

**EVIDENCE — command:** `venv/bin/pytest -q tests/test_crawler.py -k 'ac25 or ac32 or ac35'`

Cwd: `/home/moose/python/stark-web-factory-scrapper-v1`. Exit **0**. Subprocess elapsed **1.9632 s**. Exact environment/command: [metadata](evidence/retest_eee039a/targeted.command.json).

stdout: [targeted.stdout.txt](evidence/retest_eee039a/targeted.stdout.txt)

```text
...                                                                      [100%]
- generated xml file: /home/moose/python/stark-web-factory-scrapper-v1/agent_docs/ACTION/web-factory-p1-bim001/QA/evidence/retest_eee039a/targeted.xml -
3 passed, 45 deselected in 1.48s
```

stderr: [targeted.stderr.txt](evidence/retest_eee039a/targeted.stderr.txt)

```text
(empty)
```

**PASS:** `test_ac25_failed_and_exception_no_html`, `test_ac32_manifest_identity_values`, and `test_ac35_page_values_match_run_summary` all pass. **3 passed, 45 deselected in 1.48s**, 0 failures/errors. No warnings printed; stderr empty. [JUnit](evidence/retest_eee039a/targeted.xml) preserves exact case names. AC-25 is the only test referencing RaisingFakeCrawler; direct AC-71 source/helper verification is additionally captured in §4. The targeted run was green before regression.

## 8. Full regression — repo root

**EVIDENCE — command:** `venv/bin/pytest -q`

Cwd: `/home/moose/python/stark-web-factory-scrapper-v1`. Exit **0**. Subprocess elapsed **8.5749 s**. Exact environment/command: [metadata](evidence/retest_eee039a/root.command.json).

stdout: [root.stdout.txt](evidence/retest_eee039a/root.stdout.txt)

```text
......................................................                   [100%]
- generated xml file: /home/moose/python/stark-web-factory-scrapper-v1/agent_docs/ACTION/web-factory-p1-bim001/QA/evidence/retest_eee039a/root.xml -
54 passed in 8.11s
```

stderr: [root.stderr.txt](evidence/retest_eee039a/root.stderr.txt)

```text
(empty)
```

**PASS: 54 passed in 8.11s**, 0 failed/errors/skips. No warnings printed; stderr empty. Existing venv only, cwd repository root. Exact command is `venv/bin/pytest -q`. `PYTEST_ADDOPTS` adds only `-p no:cacheprovider`, a fresh QA-lane basetemp and JUnit output; `PYTHONDONTWRITEBYTECODE=1` and QA-lane TMPDIR contain temporary writes. No tests are selected out. [JUnit](evidence/retest_eee039a/root.xml).

## 9. Full regression — /tmp

**EVIDENCE — command:** `/home/moose/python/stark-web-factory-scrapper-v1/venv/bin/pytest -q /home/moose/python/stark-web-factory-scrapper-v1/tests`

Cwd: `/tmp`. Exit **0**. Subprocess elapsed **8.1294 s**. Exact environment/command: [metadata](evidence/retest_eee039a/foreign.command.json).

stdout: [foreign.stdout.txt](evidence/retest_eee039a/foreign.stdout.txt)

```text
......................................................                   [100%]
- generated xml file: /home/moose/python/stark-web-factory-scrapper-v1/agent_docs/ACTION/web-factory-p1-bim001/QA/evidence/retest_eee039a/foreign.xml -
54 passed in 7.58s
```

stderr: [foreign.stderr.txt](evidence/retest_eee039a/foreign.stderr.txt)

```text
(empty)
```

**PASS: 54 passed in 7.58s**, 0 failed/errors/skips. No warnings printed; stderr empty. Cwd is `/tmp`; the command uses the absolute existing repo venv pytest and absolute tests path, with `PYTHONPATH=<repo>`. Cache/bytecode controls and separate QA-lane basetemp/JUnit keep all QA files inside the lane. `/tmp` is execution cwd only. [JUnit](evidence/retest_eee039a/foreign.xml).

## 10. BIM000 14-test proof

**EVIDENCE:** Every baseline name occurs exactly once and passes in each full JUnit file. [baseline_passing.json](evidence/retest_eee039a/baseline_passing.json) preserves 28 case-level records (14 × two contexts), timings, assertion counts and exact-source equality. Both complete suites contain 54 passing cases; zero skipped/error/failed nodes. This is not inferred merely from the aggregate count.

| Exact BIM000 function | Current source | Root | /tmp | Preserved Assert count |
|---|---|---|---|---:|
| `test_403_and_500_write_no_file_and_are_recorded` | `tests/test_crawler.py:62` | PASS | PASS | 10 |
| `test_three_consecutive_429_stop_the_run` | `tests/test_crawler.py:84` | PASS | PASS | 5 |
| `test_blocked_counter_resets_on_success` | `tests/test_crawler.py:94` | PASS | PASS | 1 |
| `test_pause_between_pages_not_before_first` | `tests/test_crawler.py:105` | PASS | PASS | 2 |
| `test_limit_and_input_flags` | `tests/test_crawler.py:112` | PASS | PASS | 4 |
| `test_session_user_agent_on_sitemap_calls` | `tests/test_crawler.py:124` | PASS | PASS | 5 |
| `test_import_from_foreign_cwd_creates_no_directory` | `tests/test_crawler.py:143` | PASS | PASS | 1 |
| `test_empty_input_writes_truthful_summary_without_crawling` | `tests/test_crawler.py:149` | PASS | PASS | 6 |
| `test_is_valid_link_rejects_empty_mailto_tel_and_fragments` | `tests/test_discover.py:7` | PASS | PASS | 5 |
| `test_is_valid_link_accepts_relative_and_same_domain_only` | `tests/test_discover.py:15` | PASS | PASS | 3 |
| `test_normalize_link_strips_fragment_and_query_and_resolves_relative` | `tests/test_discover.py:21` | PASS | PASS | 2 |
| `test_flat_sitemap_returns_all_locs` | `tests/test_sitemap_utils.py:25` | PASS | PASS | 1 |
| `test_sitemap_index_follows_child_sitemaps` | `tests/test_sitemap_utils.py:33` | PASS | PASS | 1 |
| `test_unparseable_sitemap_returns_empty` | `tests/test_sitemap_utils.py:41` | PASS | PASS | 1 |

Total: **14 baseline functions, 47 exact preserved assertion statements**, all passing in both contexts. Foreign-CWD import-side-effect regression is included and passing.

## 11. Surface recheck

**PASS WITH NOTE — accepted AC-90 erratum applied.** Full main-to-candidate name-status evidence: [main_surface.stdout.txt](evidence/retest_eee039a/main_surface.stdout.txt), [surface_verified.json](evidence/retest_eee039a/surface_verified.json).

984 changed paths versus main, largely historical module/QA evidence. Every changed non-agent_docs file is:

```text
CHANGELOG.md
README.md
RECOVERY.md
RUN_NOTES.md
smart_crawler/crawler.py
tests/test_crawler.py
```

- Only changed implementation file: smart_crawler/crawler.py.
- Only changed existing test file: tests/test_crawler.py.
- No discover_site changes; no requirements/lock changes; no unexpected implementation files.
- README, RUN_NOTES, CHANGELOG and tests are within original BIM001 surface. RECOVERY.md is allowed by the explicitly recorded process-documentation erratum.
- Remaining paths are under agent_docs. No frozen BIM001 contract file changed since previous candidate.

**EVIDENCE — provenance limit:** Updated author history is captured in [authors_current.stdout.txt](evidence/retest_eee039a/authors_current.stdout.txt); all seven author names match the previous main-baseline identity. Repair report records read-only git, with commit/stage responsibility left to Director. As before, author strings do not authenticate who physically executed git. QA executed no mutating git.

## 12. Phase-map/doctrine intake status

**EVIDENCE — both supplied documents read completely. The previous availability GAP is closed.** Exact supplied paths:

1. `/home/moose/python/stark-web-factory-scrapper-v1/agent_docs/ACTION/web-factory-p1-bim001/QA/INPUTS/WEB_FACTORY_P1_PHASE_MAP_v0_2.md` — 98 lines, version 0.2, dated 2026-09-05, RULINGS LOCKED. [Document](INPUTS/WEB_FACTORY_P1_PHASE_MAP_v0_2.md).
2. `/home/moose/python/stark-web-factory-scrapper-v1/agent_docs/ACTION/web-factory-p1-bim001/QA/INPUTS/WEB_FACTORY_P1_DOCTRINE_JOURNAL.md` — 204 lines, version 0.2, dated 2026-09-05, OPEN. [Document](INPUTS/WEB_FACTORY_P1_DOCTRINE_JOURNAL.md).

Exact SHA-256 and paths: [context_intake.json](evidence/retest_eee039a/context_intake.json). These supplied versions are now the available context; their historical module statuses are not treated as today's candidate status.

**EVIDENCE:** Phase map §3 identifies BIM001 raw rendered HTML, manifest, stage log and typed absences; later REST/media/AI work remains outside this repair. Doctrine J-04 preserves the frozen contract while allowing append-only QA evidence/errata; J-08 retains Director git mutation authority; J-11 keeps repair on the same QA line; J-12/J-15 preserve seat separation; J-13 contains QA artifacts/provenance; J-16 calls for errata rather than rewritten acceptance wording. A new [QA_WORK_JOURNAL.md](QA_WORK_JOURNAL.md) records this retest's helpers, evidence families and process-local state substitutions after reading J-13. It does not rewrite earlier evidence.

## 13. Updated AC status table

All **52** ACs are listed. This bounded retest incorporates the previous independent Stage 2 evidence on `657e25e…` for unchanged behavior, the exact limited repair diff, fresh affected tests and both complete regressions on `eee039a…`, and the authoritative supplied rulings. Old manual artifacts retain their original provenance and are not presented as newly generated on the repaired SHA. No full Stage 2 re-execution or live smoke is implied.

| AC | Status | Evidence basis / remaining clause |
|---|---|---|
| AC-01 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-02 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-03 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-04 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-05 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-06 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-07 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-08 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-10 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-11 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-12 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-13 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-14 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-20 | PASS WITH NOTE | Supplied meaningful/nonempty interpretation recorded; prior empty-HTML evidence matches; behavior unchanged. |
| AC-21 | UNTESTED | Offline exact-byte evidence passes; required live size clause remains unexecuted. |
| AC-22 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-23 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-24 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-25 | PASS | Fresh affected test and both full suites pass using BIM001-only RaisingFakeCrawler. |
| AC-26 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-27 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-28 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-30 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-31 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-32 | PASS | Fresh persisted port manifest and sorted/unique/None/case probes pass; extended test green. |
| AC-33 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-34 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-35 | PASS WITH NOTE | I-2 accepted erratum recorded; prior collision evidence matches unchanged writer; affected test green. |
| AC-36 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-37 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-40 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-41 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-42 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-43 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-50 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-51 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-60 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-61 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-62 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-63 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-70 | PASS | All 14 names/47 baseline assertions preserved; 14 passing cases in each fresh full suite. |
| AC-71 | PASS | FakeCrawler exact main equivalence; only a/b/c baseline amendments; BIM001 exception seam verified. |
| AC-72 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-73 | PASS | Fresh root 54 passed and /tmp 54 passed; exact contexts and JUnit retained. |
| AC-74 | UNTESTED | Not authorized or executed; all live smoke requirements remain untested. |
| AC-80 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-81 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-82 | PASS | Fresh changelog amendment-boundary and hostname repair inspection passes; required topics retained. |
| AC-90 | PASS WITH NOTE | Fresh full surface within original paths plus accepted RECOVERY.md process-file erratum. |
| AC-91 | PASS | Prior independent offline evidence retained for unchanged behavior; complete repaired-candidate regression green. |
| AC-92 | PASS WITH NOTE | I-1 accepted metadata-only lookup; source-only grep matches allowed line; no caches deleted. |
| AC-93 | PASS | Previous report-command audit plus current author history/repair-report read; repository evidence only, no actor authentication. |

**Counts:** **PASS: 46**, **FAIL: 0**, **UNTESTED: 2**, **BLOCKED: 0**, **PASS WITH NOTE: 4**. Total **52**.

## 14. Readiness recommendation for live AC-74

**Recommend that SOL authorize the controlled live AC-74 smoke on `eee039a2c06cc1aeaa3e9dfb52e17db9d3ddb20a`.** Both required repairs independently pass, the four rulings are fully recorded and match evidence, both regressions are green with all 14 BIM000 cases proven, the accepted surface is respected, and the supplied context gap is closed.

Remaining **FAIL: 0**. Remaining **UNTESTED: 2** — AC-21's live byte-size clause and AC-74. No live CyberizeGroup command was executed during this retest. SOL must explicitly authorize the live run before it occurs. The future run should reverify the discovery input count/target identity, preserve the pinned specimen, capture a fresh capped run, and record the missing live artifact evidence.

This is Cody's readiness recommendation, not SOL's Gate Q verdict or BIM001 certification. Tony retains all git authority.
