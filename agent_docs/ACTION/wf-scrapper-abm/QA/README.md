# QA lane — wf-scrapper-abm

Owner: SOL (QA Lead). Executor: Cody. Opens when the Director creates `qa/wf-scrapper-abm` from the engineering candidate (J-18). Nothing here is written during Block 2.

- `MASTER_QA_TEST_PLAN.md` — 10x Lab v0.2 design, 24 groups, all 48 ACs. SOL binds §"Execution binding sheet" at QA entry from repo truth and the candidate SHA. QA does not consume the engineer's fixtures as proof; it may reuse them as inputs after inspection.
- `QA_WORK_JOURNAL.md` — Cody creates it the first time he creates a helper (J-13).
- Cold-read (AC-035, R5): SOL schedules a fresh Fable session, hands it only the pack folder, records answers and judgment here.
- Post-Gate-Q: QA Cleanup (J-19) → durable package selected and committed by the Director → Working Tree CLEAN → Architect closeout instruction.
