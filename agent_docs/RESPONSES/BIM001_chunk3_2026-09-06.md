# BIM001 — Chunk 3 report — capture in the crawl loop — 2026-09-06

**Plain language (3 lines).**
The crawl loop can now capture raw HTML: `crawl_page` carries the page's `html` (guarded, may be absent) and a `fetched` flag out alongside the markdown, and `crawl_all` accepts an optional run folder — when given, every attempted page is classified (blocked / failed / unsupported / captured), its HTML saved byte-for-byte under a collision-safe slug, and a manifest entry plus a stage-log line recorded. Without a run folder the loop behaves exactly as bim000, which is what the 14 old tests still exercise. `main()` does not pass a run folder yet — that is chunk 4 — so the live command is still pure bim000.

**What changed (file:function).**
- `smart_crawler/crawler.py` — `crawl_page`: every return now includes `html` (`getattr(result, "html", None)`) and `fetched`; the record dict is unchanged. New `record_capture(run, page, url, html, fetched, md_saved)`: outcome ladder, `allocate_slug` only for captured pages, `save_html` in a try/except (AC-28), `md_file` = the markdown file bim000 actually wrote (I-2), one stage-log line per page. `crawl_all(urls, crawler, run=None)`: pops the three transport keys before `pages.append` (AC-60), calls `record_capture` when `run` is given, logs `stop rule` on early stop. Untouched: `save_markdown` (body byte-identical to `main`, verified by diff), `write_summary`, `run`, `main`, all `CrawlerRunConfig` literals (each grep = 1).
- `tests/test_crawler.py` — **AC-71(c) amendment:** `_result()` gains optional `html=None`, attribute set only when provided; `FakeCrawler.arun` raises when handed an Exception (helper only, no bim000 test changes). 7 new tests: `test_ac20_captured_for_successful_fetch`, `test_ac22_html_stem_equals_md_stem` (also `def slugify` count = 1 from source), `test_ac24_blocked_no_html`, `test_ac25_failed_and_exception_no_html`, `test_ac26_unsupported_when_html_absent`, `test_ac27_html_independent_of_markdown`, `test_ac28_write_failure_is_failure_not_crash`.
- `CHANGELOG.md` — chunk 3 line; AC-71(c) listed by test/helper name.

**pytest:** `venv/bin/pytest -q` → `37 passed in 2.96s`.

**diff stat:**
```
 CHANGELOG.md             |  2 +
 smart_crawler/crawler.py | 68 ++++++++++++++++++++++++++++-----
 tests/test_crawler.py    | 98 ++++++++++++++++++++++++++++++++++++++++++++++--
 3 files changed, 155 insertions(+), 13 deletions(-)
```
`git diff main -- tests/` → no removed `assert`. `save_markdown` body diff vs `main` → identical. AC-62 greps → 1 each. AC-92 → still the single accepted metadata hit.

**To do:** Director stages chunk 3. On "next" → chunk 4 (wire `main()`: run folder, empty input, counts/hosts, absences, manifest on all paths; sandbox amendment AC-71a).

**chunk 3 ready to stage**
