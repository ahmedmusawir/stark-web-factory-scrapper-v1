# BIM001 — Chunk 5 report — hardening — 2026-09-06

**Plain language (3 lines).**
No product behavior changed in this chunk. Five tests now pin the contract points QA will grep for — run-folder layout end to end, nothing called at import time (checked via the AST, not just a grep), the untouched `run_summary.json` key sets through the real `main()`, the bim000 run-config literals and `save_markdown` body, and the complete exit-code matrix (0 / 1 / 2 on every path). An import-usage sweep found every import in `crawler.py` used and `load_urls` still legitimately alive as the bim000 API. The one failure on the first run was my test helper creating a folder twice; fixed in the helper.

**What changed (file:function).**
- `tests/test_crawler.py` — 5 new tests: `test_ac10_run_folder_layout_end_to_end`, `test_ac12_no_mkdir_at_module_level`, `test_ac60_run_summary_contract_unchanged_via_main`, `test_ac61_62_bim000_code_and_config_literals_unchanged`, `test_ac63_exit_codes_complete`. Helper `_one_url_input`: `mkdir(exist_ok=True)`.
- `CHANGELOG.md` — chunk 5 line.
- `smart_crawler/crawler.py` — **not touched** in this chunk.

**pytest:** `venv/bin/pytest -q` → `54 passed in 7.68s` (14 bim000 + 40 bim001 items; AC-72 minimum 14 met more than twice over; AC-73 N ≥ 28 met).

**Spec greps, preview of P3 (all read-only):**
- AC-22 `def slugify` → 1 · AC-62 four literals → 1 each · AC-70 removed bim000 asserts → 0
- AC-90 surface (`git diff --name-only main`, non-agent_docs) → `CHANGELOG.md`, `smart_crawler/crawler.py`, `tests/test_crawler.py`; requirements/.env.example diff → 0
- AC-91 `pip freeze` vs lock → 0 diff lines; `pip check` → No broken requirements found
- AC-92 → 1 hit, `crawler.py:285` metadata lookup (I-1 accepted; erratum at QA)
- AC-93 authors main..HEAD → Director only
- `--help` shows `--project`, `--input`, `--limit`

**diff stat:**
```
 CHANGELOG.md          |  1 +
 tests/test_crawler.py | 68 ++++++++++++++++++++++++++++++++++++++++++++++++++-
 2 files changed, 68 insertions(+), 1 deletion(-)
```

**To do:** Director stages chunk 5. On "next" → chunk 6, docs (README two-command section with `--project`, RUN_NOTES bim001 section + two-file-state sentence, CHANGELOG close of the bim001 entry incl. "no pin changes").

**chunk 5 ready to stage**
