# BIM001 — Chunk 2 report — RunFolder — 2026-09-06

**Plain language (3 lines).**
The run-folder machinery now exists as one class plus two helpers: it creates `outputs/<project>/runs/<run_id>/html/`, appends timestamped stage-log lines, hands out `-2`/`-3` slugs on collision, writes HTML byte-for-byte and refuses to overwrite, and writes the 23-key manifest and the absences list. It is not called from `main()` yet, so the live crawler behaves exactly as bim000 until chunk 4. One test constant was wrong on the first run (I counted `É` as one byte; it is two) — fixed in the test, no code change.

**What changed (file:function).**
- `smart_crawler/crawler.py` — new `RunFolder` (`__init__`, `_set_started`, `run_dir_rel`, `create` with the I-3 same-second wait, `log`, `allocate_slug`, `save_html`, `record_page`, `write_manifest`, `write_absences`), new `build_absences()`, new `input_hosts()`; constants `MANIFEST_SCHEMA`, `ACCESS_RUNG`, `OUTCOMES`; imports `platform`, `time`, `urlparse`. `playwright_version` comes from `importlib.metadata` — the one accepted AC-92 hit (I-1). The three run-config mirrors are literals with a comment (AC-62). No existing function touched.
- `tests/test_crawler.py` — 8 new tests: `test_ac10_run_folder_layout_exact`, `test_ac21_html_byte_for_byte` (also CRLF/whitespace untouched), `test_ac23_collision_suffix` (3 URLs → `<slug>.html`, `-2`, `-3`, 3 distinct bodies, manifest maps each URL, overwrite raises), `test_ac30_manifest_top_level_keys_exact`, `test_ac31_manifest_fixed_values`, `test_ac34_manifest_page_keys_exact`, `test_ac40_absences_shape`, `test_ac50_stage_log_lines_start_with_timestamp`.
- `CHANGELOG.md` — chunk 2 line added under the open bim001 entry.

**pytest:** `venv/bin/pytest -q` → `30 passed in 2.67s`.

**diff stat:**
```
 CHANGELOG.md             |   1 +
 smart_crawler/crawler.py | 126 ++++++++++++++++++++++++++++++++++++++++++++++
 tests/test_crawler.py    | 128 +++++++++++++++++++++++++++++++++++++++++++++++
 3 files changed, 255 insertions(+)
```
`grep -n mkdir` → lines 180 (`write_summary`), 221 (`RunFolder.create`), 418 (`main`) — all inside functions. AC-92 grep → 1 hit, line 275, the metadata lookup (I-1 accepted; erratum at QA). `git diff main -- tests/` → no removed `assert`.

**To do:** Director stages chunk 2. On "next" → chunk 3 (capture in the crawl loop: `crawl_page` html/fetched, `crawl_all` outcome ladder, stub amendment AC-71c).

**chunk 2 ready to stage**
