# Response — bim000 CP2 (Stage 2): Stage prep — PLAN

**Status:** PENDING_APPROVAL
**Module:** web-factory-p1-bim000 · Stage 2 · Plan Mode. No edit performed.
**Branch (read-only):** `web-factory-p1-bim000` — HEAD `ba05030 "bim000 ready to start"`. Pins: `crawl4ai==0.9.3`, `playwright==1.52.0`; installed crawl4ai 0.9.3. Pins will not be touched.

---

## ⚠️ Discrepancy to resolve first (Confusion Management)

CP2 says *"Stage 1 is green and committed."* `git status` (read-only) shows the Stage 1 diff is **not committed**: ` M requirements.txt`, ` M requirements-lock.txt`, plus the two Stage 1 response files (`..._122244_bim000-cp1-plan.md`, `..._123610_bim000-stage1-upgrade.md`) untracked, and ` M RECOVERY.md`, ` M session_2026-09-05.md`. HEAD `ba05030` predates them. Also the module `CLAUDE.md` "Current stage" line still reads "Stage 1 — CP1. Waiting for Director approval" (frozen folder — I will not edit it).
**Why it matters:** CP2 asks for one commit-sized chunk each. If Stage 1 is uncommitted when chunk A lands, the Director's first Stage 2 commit would carry the pin bump inside it. **Recommendation:** commit Stage 1 before I start chunk A. I will not run it:
```
git add requirements.txt requirements-lock.txt agent_docs/RESPONSES/response_2026-09-05_122244_bim000-cp1-plan.md agent_docs/RESPONSES/response_2026-09-05_123610_bim000-stage1-upgrade.md RECOVERY.md agent_docs/SESSIONS/session_2026-09-05.md
git commit -m "bim000 stage 1: crawl4ai 0.6.3 -> 0.9.3, regression + smoke green"
```
If you prefer to proceed anyway, say so and I will start; the chunks remain separable by file.

## Second small drift (AC-45)

CP2 names **four** stale `RESPONSES/` references. On disk today there are **three**: `session_2026-09-02.md:41`, `session_2026-09-02.md:47`, `session_2026-09-03.md:30`. `RECOVERY.md:5` no longer contains the stale path — RECOVERY.md was rewritten during CP1 close-out and now points at current artifacts. I will repoint the three and record the fourth as already superseded. EVIDENCE: `grep -n "RESPONSES/response_2026-09-02" RECOVERY.md agent_docs/SESSIONS/*.md | grep -v OLD/`.

---

## Read-only survey (what the plan is built on)

- `discovered_pages_final` occurrences: `README.md:33,36`, `RUN_NOTES.md:36`, `smart_crawler/crawler.py:9,28`, `discover_site/smart_discover.py:23` (file to be deleted). Stray artifact `outputs/discovered_pages_final.json` present from the CP1 smoke.
- `smart_discover` references outside `agent_docs/`: `README.md:45,58` + the file itself. **No Python import of it anywhere** (grep over `*.py` → only its own usage strings). Safe to delete.
- Stale strings in `crawler.py`: `:3` "v0.6.x", `:5` + `:77` + `:195` "AI-cleaned", `:47` + `:190` "v0.7.x", `:218` "707", `:206` `input()` prompt, `:24` `Path.cwd()`, `:27` import-time `mkdir`.
- `discover.py:1` `import sys` unused. Test fixtures: `cyberizegroup` 7× in `test_discover.py`, 12× in `test_sitemap_utils.py`.
- `CHANGELOG.md` absent. `CLAUDE.md` layout table rows at `:299-315`; tree block at `:297-305`.
- **Identity check on installed 0.9.3 (EVIDENCE):** for UA `Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/140.0.0.0 Safari/537.36`, `UAGen.generate_client_hints()` → `"Chromium";v="140", "Not_A Brand";v="8", "Google Chrome";v="140"` (non-empty; AC-31 satisfiable).

---

## PLAN — chunks in CP2 order, `venv/bin/pytest -q` after every chunk

**A. Handoff contract (F3)** — `smart_crawler/crawler.py`
- `argparse`: `--input PATH` (default `REPO_ROOT/outputs/discovered_pages.json`), `--limit N` (default all, must be ≥1). Remove the `input()` prompt entirely; no `--yes` (default runs without input, per CP2).
- `load_urls(path, limit)` returns the first N. Remove every `discovered_pages_final` string. Docstring rewritten to the two-command flow.
- Delete stray `outputs/discovered_pages_final.json` (gitignored artifact).
- Also purge the string from `README.md:33-37` and `RUN_NOTES.md:36` here (AC-13 grep must hit 0) — RUN_NOTES §4 gets a one-line note that the trim step is retired in bim000; the full docs rewrite is chunk G.

**B. Status validation** — `crawler.py`
- After `arun`: `status = result.status_code`, `ok = result.success and status is not None and status < 400`. Failed → no `.md`, `error` = `"blocked"` if status in (403, 429) else `result.error_message` or `f"HTTP {status}"`.
- Consecutive-blocked counter: reset on ok; at 3 → print `"⛔ 3 consecutive blocked pages — stopping run"`, break, still write summary, exit code 2.
- Print one status line per page: `[i/N] 200 ok 6.4s url` / `[i/N] 403 BLOCKED url`.
- `outputs/run_summary.json` = `{started_at, finished_at, crawl4ai_version, pause_range_s: [2, 5], pages: [{url, status, ok, elapsed_s, error}]}` (ISO-8601 UTC timestamps, `elapsed_s` rounded to 0.1). Written on every run, including early stop.

**C. Pacing and identity** — `crawler.py`, `discover_site/sitemap_utils.py`, `discover_site/discover.py`
- `await asyncio.sleep(random.uniform(2, 5))` between pages, not before the first, logged `"⏸ pause 3.4s"`. `PAUSE_RANGE_S = (2, 5)` constant (also written to summary).
- `USER_AGENT` constant = the Chrome/140 string above. **Single source of truth:** defined in `discover_site/sitemap_utils.py` next to a module-level `SESSION = requests.Session(); SESSION.headers["User-Agent"] = USER_AGENT`; `discover.py` and `sitemap_utils.py` call `SESSION.get(...)` everywhere; `crawler.py` imports `USER_AGENT` from there for `BrowserConfig(user_agent=...)`. (Alternative if you dislike the cross-package import: duplicate the constant in `crawler.py` — say so.)
- `BrowserConfig(headless=True, user_agent=USER_AGENT)` only. `CrawlerRunConfig` keeps `cache_mode=BYPASS, page_timeout=90000, delay_before_return_html=3.0, wait_for_images=True`. No `enable_stealth`, `use_undetected`, `override_navigator`, `magic`, `simulate_user` anywhere (AC-33 grep = 0).
- After the change: print `UAGen.generate_client_hints(USER_AGENT)` once via a read-only one-liner for the report (not in product code).

**D. Truth (F4) + retirement (R-B)** — `crawler.py`, delete `discover_site/smart_discover.py`, `README.md`
- Banner: `f"Batch crawler — crawl4ai {importlib.metadata.version('crawl4ai')}"`; `f"Ready to crawl {len(urls)} URLs"`; ETA line from `len(urls)` × observed ~7 s + pauses, or dropped. Remove "AI-cleaned", "v0.6.x/v0.7.x", "707", "UPGRADED". Docstring states raw_markdown fallback honestly (fit_markdown needs a content filter — F1 deferred).
- Confirm no import of `smart_discover` (done, survey above) → delete file → remove `README.md:45` paragraph and `:58` mention.

**E. Paths and hygiene** — `crawler.py`, `discover.py`, `tests/*`
- `REPO_ROOT = Path(__file__).resolve().parents[1]`; `OUTPUT_DIR = REPO_ROOT / "outputs"`; `PAGE_DIR.mkdir(...)` moved into `main()`. Nothing runs at import time except constant definitions.
- `discover.py`: drop `import sys`. Tests: `cyberizegroup.com` → `example.com` (19 occurrences, both files).

**F. Tests** — new `tests/test_crawler.py`; edits to `tests/test_sitemap_utils.py`
- Design for testability: `crawl_all(urls, crawler, pause=...)` takes an object with `async arun(url, config)`; tests pass a `FakeCrawler` returning stub results (`status_code`, `success`, `error_message`, `markdown.raw_markdown`). `asyncio.sleep` and `random.uniform` monkeypatched to no-op/fixed. Output dir redirected via monkeypatching `crawler.PAGE_DIR`/`SUMMARY_PATH` to `tmp_path`.
- Tests: (1) 403 → no file, summary `ok=False, error="blocked"`; 500 → no file, `ok=False, error="HTTP 500"`; (2) three consecutive 429 → run stops after 3, summary has 3 entries, message printed; (3) `--limit 3` → 3 URLs loaded; `--input custom.json` honored; (4) `SESSION.headers["User-Agent"] == USER_AGENT` and `fetch_sitemap_urls` calls go through `SESSION.get` (stub `SESSION.get`, assert UA on the session); (5) importing `smart_crawler.crawler` with CWD = `tmp_path` (subprocess `python -c "import smart_crawler.crawler"`, `cwd=tmp_path`, `PYTHONPATH` unset — module found via `-c` + `sys.path` insert of repo root in the command string) creates no directory in `tmp_path`.
- Existing 6 stay; the sitemap tests switch from stubbing `requests.get` to stubbing `SESSION.get`. Expected total: 6 + 7 = **13** (state actual count).

**G. Docs** — `README.md`, `RUN_NOTES.md`, new `CHANGELOG.md`, `agent_docs/SESSIONS/session_2026-09-02.md`, `session_2026-09-03.md`, root `CLAUDE.md`
- README "Run": exactly two commands (`discover <url>` → `crawler [--limit N] [--input PATH]`), `run_summary.json` described, layout block updated (no smart_discover, `tests/test_crawler.py`).
- RUN_NOTES: new "## 7. bim000 (2026-09-05)" section: Stage 1 pin bump evidence + Stage 2 two-command smoke transcript.
- `CHANGELOG.md` created with header + one entry `## 2026-09-05 — [CC] bim000` covering Stage 1 (pin) and Stage 2 (each chunk one line). Format per CLAUDE.md Changelog Protocol.
- Repoint 3 stale refs to `agent_docs/RESPONSES/OLD/...`; note RECOVERY.md already superseded.
- `CLAUDE.md`: one tree line `│   ├── OLD/  ← archived responses, Director-managed` under `RESPONSES/` and one table row `| Archived responses | agent_docs/RESPONSES/OLD/ | Director moves closed-module responses here |`. Nothing else in CLAUDE.md changes. CHANGELOG entry also records this doc edit per Changelog Protocol.

**H. Final smoke** — from repo root, no manual step, no wrapper:
`printf '1\n' | venv/bin/python -m discover_site.discover https://cyberizegroup.com` → `venv/bin/python -m smart_crawler.crawler --limit 10`. Must be 10/10, `run_summary.json` 10 × 200, 9 pause lines in 2–5 s. Also AC-10 check: run discover from `/tmp` and confirm the file lands in repo `outputs/`.

**I. Report** — `agent_docs/RESPONSES/response_<ts>_bim000-stage2-prep.md` mapping AC-10…AC-53 to evidence. Session log per chunk; RECOVERY.md at close.

---

## FILES I WILL TOUCH

**Modify:** `smart_crawler/crawler.py` · `discover_site/discover.py` · `discover_site/sitemap_utils.py` · `tests/test_discover.py` · `tests/test_sitemap_utils.py` · `README.md` · `RUN_NOTES.md` · `CLAUDE.md` (root; two lines) · `agent_docs/SESSIONS/session_2026-09-02.md` (2 path strings) · `agent_docs/SESSIONS/session_2026-09-03.md` (1 path string) · `agent_docs/SESSIONS/session_2026-09-05.md` · `RECOVERY.md`
**Create:** `tests/test_crawler.py` · `CHANGELOG.md` · `agent_docs/RESPONSES/response_<ts>_bim000-stage2-prep.md` · run artifacts `outputs/run_summary.json`, `outputs/pages/*.md` (gitignored)
**Delete:** `discover_site/smart_discover.py` · `outputs/discovered_pages_final.json` (gitignored artifact)
**Will NOT touch:** `requirements.txt` · `requirements-lock.txt` · `.env.example` · `pytest.ini` · `.gitignore` · `venv/`, `venv.bak/` · `agent_docs/ACTION/**` · `agent_docs/SKILLS/**` · `agent_docs/RESPONSES/OLD/**` · `agent_docs/RECON/**`

## ASSUMPTIONS
1. Stage 1 gets committed before chunk A (see discrepancy) — or you tell me to proceed regardless.
2. `USER_AGENT` lives in `discover_site/sitemap_utils.py` and `crawler.py` imports it (one definition). Alternative on request: duplicate constant.
3. Chrome/140 is "current" enough for the UA string; the token set is what AC-31 checks.
4. Exit code 2 on the 3-consecutive-blocked stop; 0 otherwise (even with some failed pages — the summary carries the truth).
5. "Four stale references" = three on disk + RECOVERY.md already superseded (AC-45 evidence will say so).
6. The `--yes` flag is not added (CP2 wording: default must run without input).

## RISKS
- The sitemap tests change their stub target (`SESSION.get` instead of `requests.get`); I will keep assertions identical so they still prove the same behavior.
- Pressable could 429 the final smoke → status validation records it, run continues past a single block; if 3 in a row, the run stops by design and I report it (brief §7).
- `wait_for_images=True` stays (ruled); smoke will again log "images failed to load" — expected, not a failure.

→ Awaiting approval. Please also answer the Stage-1-commit question.

---

## As printed on screen (12:53) — condensed version of the plan above

**Branch:** `web-factory-p1-bim000`, HEAD `ba05030`. Pins 0.9.3, untouched.

**⚠️ Discrepancy:** CP2 says Stage 1 is committed; git status shows the pin bump + Stage 1 reports uncommitted. Recommended Director commit first (commands in the section above). Smaller drift: 3 of the 4 named stale RESPONSES refs exist; RECOVERY.md already superseded.

**Plan:** A handoff (`--input`, `--limit`, no prompt, purge `_final`) · B status validation (≥400 or not success = failed, no file; 403/429 = blocked; 3 consecutive → stop, exit 2; `run_summary.json`) · C pacing 2–5 s + Chrome/140 UA (non-empty sec-ch-ua verified) + one `requests.Session` · D truthful banner from package metadata, delete `smart_discover.py` · E root-anchored paths, mkdir in main, drop `import sys`, example.com fixtures · F `tests/test_crawler.py` (expected 13 total) · G README/RUN_NOTES/CHANGELOG, 3 repoints, CLAUDE.md OLD row · H two-command smoke 10/10, 9 pauses, plus run from `/tmp` · I report AC-10…AC-53.

**Touch:** crawler.py, discover.py, sitemap_utils.py, both tests, README, RUN_NOTES, root CLAUDE.md (2 lines), sessions 09-02/09-03/09-05, RECOVERY.md; create test_crawler.py, CHANGELOG.md, report; delete smart_discover.py + stray final json. **Not touch:** requirements*, .env.example, pytest.ini, .gitignore, venvs, ACTION, SKILLS, RESPONSES/OLD, RECON.

**Confirm:** (1) UA constant lives once in `sitemap_utils.py`, crawler imports it; (2) no `--yes` flag.

→ Awaiting approval + Stage 1 commit answer.
