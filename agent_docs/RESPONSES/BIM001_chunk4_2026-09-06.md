# BIM001 — Chunk 4 report — main() wired — 2026-09-06

**Plain language (3 lines).**
The crawler now produces the bim001 run folder end to end: after `--project` validation and input read, `main()` creates `outputs/<project>/runs/<run_id>/`, logs `run start`, crawls (or takes the empty-input branch), then writes `run_summary.json` exactly as bim000 did, plus `manifest.json`, `absences.json` and the closing `run end` line — on normal completion, on empty input, and on the 3-consecutive-blocked stop (manifest first, then exit 2). The sandbox fixture now redirects the run root to tmp (the last of the three permitted bim000 test amendments). Offline tests drive the real `main()` with a fake browser, so the whole path is exercised without network.

**Git note for the Director.** `git log -1` shows `e777e4e 6sep2026 - bim001 chunk 2 done`; `record_capture` (chunk 3) is not in HEAD's `crawler.py`. Either chunk 3's commit did not land or it is on another ref. The working tree carries chunks 3 + 4 together; the diff stat below is therefore against the chunk 2 commit. Not a blocker for me; please check before staging chunk 4.

**What changed (file:function).**
- `smart_crawler/crawler.py` — `run(urls, run_folder=None)` passes the RunFolder into `crawl_all`. `main()`: `read_urls` for the full list, slice by `--limit`, `command` reconstructed as `python -m smart_crawler.crawler <argv[1:]>` (I-4), `RunFolder(...).create()` immediately after input read (I-3 wait honoured; `started_at` taken from the folder), `run start` log line, inner `close_run(pages, finished_at, stopped_early)` that writes summary → manifest → absences → `run end` with identical timestamps for summary and manifest (AC-36); empty-input branch uses `close_run([], …)` (AC-13); banner shows project / run_id / run folder; totals line adds HTML captured / not attempted; `sys.exit(2)` still last. Module docstring updated. Untouched: `save_markdown`, `write_summary`, `crawl_page`, run-config literals, `load_urls`.
- `tests/test_crawler.py` — **AC-71(a):** `sandbox` fixture adds `monkeypatch.setattr(crawler, "RUN_ROOT", tmp_path)`. New helpers `FakeBrowser` (stands in for `AsyncWebCrawler`), `_main()`, `_load()`. 12 new tests: `test_ac07_project_case_preserved`, `test_ac13_empty_input_creates_run_folder`, `test_ac14_two_runs_two_folders` (exercises the same-second wait), `test_ac32_manifest_identity_values`, `test_ac33_manifest_counts`, `test_ac35_page_values_match_run_summary`, `test_ac36_timestamps_identical`, `test_ac37_manifest_on_early_stop_exit_2`, `test_ac41_absences_completeness_and_order`, `test_ac42_skipped_reasons_limit_and_stop_rule`, `test_ac43_absence_reason_matches_manifest`, `test_ac51_stage_log_contents`.
- `CHANGELOG.md` — chunk 4 line; AC-71(a) listed. All three permitted amendments are now listed (a, b, c).

**pytest:** `venv/bin/pytest -q` → `49 passed in 5.73s` (14 bim000 + 35 bim001 items; the ~6 s is the deliberate same-second wait in ac14/ac33/ac37).

**diff stat (against HEAD = chunk 2 commit, so chunk 3 is included):**
```
 CHANGELOG.md             |   4 +
 smart_crawler/crawler.py | 122 +++++++++++++++++----
 tests/test_crawler.py    | 275 ++++++++++++++++++++++++++++++++++++++++++++++-
 3 files changed, 374 insertions(+), 27 deletions(-)
```
`git diff main -- tests/` → no removed `assert`. `grep -n mkdir` → lines 190, 231, 490, all inside functions.

**To do:** Director checks the chunk 3 commit state, then stages chunk 4. On "next" → chunk 5 (hardening: spec greps, ac63 complete, ac10/12/60/61/62 end-to-end, dead-code check).

**chunk 4 ready to stage**
