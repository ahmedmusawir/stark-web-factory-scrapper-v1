# BIM001 — Chunk 6 report — docs — 2026-09-06

**Plain language (3 lines).**
The three docs now tell the truth about bim001: the README's Run section shows the required `--project`, the canonical command, the run-folder layout, the four outcomes and the exit codes; RUN_NOTES gains a bim001 section with the same canonical command, what a run leaves behind, the two-file-state sentence, the negative CLI checks and the no-pin-change statement; the CHANGELOG bim001 entry is closed with a one-bullet summary. No code was touched, so the suite is unchanged at 54. The Architect's nit is applied: the mislabeled "AC-13" grep line is removed from the chunk 5 report (bim001's AC-13 is the empty-input run folder; the `discovered_pages_final` grep was bim000's AC-13).

**What changed (file:section).**
- `README.md` — "## Run": step 2 now requires `--project`, shows the exact line `python -m smart_crawler.crawler --project CyberizeGroup --limit 10`, the run-folder tree, outcome definitions, the unchanged bim000 behaviour bullets (with the two-file-state pointer), and an exit-code line. "## Tests" and "## Layout" updated for the run folder and the `test_acNN_` naming. Intro line updated.
- `RUN_NOTES.md` — new "## bim001 — Raw HTML Capture (2026-09-06)": canonical command block, what a run leaves behind, **two-file state** ("`manifest.json` is the run record from bim001 onward; `run_summary.json` is the frozen bim000 contract retained for compatibility"), negative CLI checks, pins (no change; I-1 metadata note), pointer to the P3 transcript.
- `CHANGELOG.md` — bim001 entry title finalised; chunk 6 line; summary bullet naming `--project` flag, run folder layout, HTML capture, manifest, absences, stage log, the three AC-71 amendments (a/b/c), 40 new tests, and "no pin changes".
- `agent_docs/RESPONSES/BIM001_chunk5_2026-09-06.md` — nit: AC-13 line dropped.

**Checks (read-only):** AC-80 canonical line → README 1, RUN_NOTES 1, missing-project error output 1 (probe exited 2 and created nothing under `outputs/`). AC-81 sentence present. AC-82 entry mentions every required item. `smart_crawler/` and `tests/` diff → empty.

**pytest:** `venv/bin/pytest -q` → `54 passed in 7.58s`.

**diff stat:**
```
 CHANGELOG.md |  6 ++++--
 README.md    | 41 +++++++++++++++++++++++++++++------------
 RUN_NOTES.md | 34 ++++++++++++++++++++++++++++++++++
 3 files changed, 67 insertions(+), 14 deletions(-)
```

**To do:** Director stages chunk 6. All six chunks are then committed → hand me **P3** (regression from root and `/tmp`, pins, greps, negative CLI probes, live capped smoke, completion report).

**chunk 6 ready to stage**
