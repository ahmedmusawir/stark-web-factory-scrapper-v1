# BIM001 — Chunk 1 report — CLI + input split — 2026-09-06

**Plain language (3 lines).**
The crawler now has a `--project` flag that argparse accepts but `main()` validates first: missing or malformed → three-line corrective usage on stderr and exit 2, before any input is read, any folder made, or any browser started. `load_urls` was split so a new `read_urls` returns the full list (needed later for `input_total`), with `load_urls` behaving exactly as before. A `RUN_ROOT` constant and a `make_run_id` helper are in place for the run folder work in chunks 2–4; nothing creates folders yet.

**What changed (file:function).**
- `smart_crawler/crawler.py` — constants `RUN_ROOT = OUTPUT_DIR`, `PROJECT_RE`, `CANONICAL_EXAMPLE`, `HELP_HINT`; new `make_run_id`, `validate_project`, `print_project_usage`, `read_urls`; `load_urls` now `read_urls` + slice (same inputs, outputs, exit codes); `parse_args` +`--project` (default `None`); `main()` validates the project immediately after `parse_args` and exits 2 on `required`/`invalid`. Description string gains "+ raw HTML". No other function touched (`save_markdown`, `write_summary`, `crawl_page`, `crawl_all`, `run` unchanged).
- `tests/test_crawler.py` — 7 new tests: `test_ac01_project_flag_optional_to_parser`, `test_ac02_missing_project_refuses_before_crawl` (browser + `read_urls` patched to fail; tmp tree unchanged), `test_ac03_04_05_missing_project_message_lines`, `test_ac06_invalid_project_space_and_dotdot` (parametrized ×2; also 64/65-char boundary), `test_ac08_help_and_limit_zero_preserved`, `test_ac11_run_id_format_matches_started_at`, `test_ac63_exit_codes_missing_input_and_project`.
- **AC-71(b) amendment, pulled forward from chunk 4:** `test_empty_input_writes_truthful_summary_without_crawling` argv gains `--project TestProj` because validation-first made it exit 2. Assertions untouched (`git diff main -- tests/` shows no removed `assert`).
- `CHANGELOG.md` — bim001 section opened; the amendment is listed by test name as the rules require.

**pytest:** `venv/bin/pytest -q` → `22 passed in 2.56s` (14 bim000 + 8 new items; `ac06` is parametrized ×2).

**diff stat:**
```
 CHANGELOG.md             |  7 ++++
 smart_crawler/crawler.py | 56 +++++++++++++++++++++++----
 tests/test_crawler.py    | 98 +++++++++++++++++++++++++++++++++++++++++++++++-
 3 files changed, 153 insertions(+), 8 deletions(-)
```
`grep -n mkdir smart_crawler/crawler.py` → lines 177 (`write_summary`) and 292 (`main`) only, both inside functions.

**To do:** Director stages chunk 1. On "next" → chunk 2 (RunFolder class, pure + tmp-tested).

**chunk 1 ready to stage**
