# READ — pre-bim001 — 2026-09-06

> Read-only. Claudy, Engineer seat. Branch `web-factory-p1-bim001` (EVIDENCE: `git branch --show-current`), HEAD `57a0f59 6sep2026 - bim000 close out` (EVIDENCE: `git log -1 --oneline`). Working tree: only today's session log untracked. No edits, no installs, no network, no mutating git.
> Labels: **EVIDENCE** (file:line or command output) · **INFERENCE** (basis stated) · **GAP** (expected, absent).

---

## 1. `smart_crawler/crawler.py` (300 lines)

### Functions

| Function | Lines | What it does |
|---|---|---|
| `slugify(text)` | 52-54 | `re.sub(r"[^a-z0-9]+", "-", text.strip().lower()).strip("-") or "root"`. Lowercases, collapses every non-alphanumeric run to one `-`, trims, falls back to `"root"`. EVIDENCE. INFERENCE: `https://a.com/x/` and `https://a.com/x` and `HTTPS://A.COM/X` all map to `a-com-x` — collisions are possible by design (no hash, no uniqueness check). |
| `utc_now()` | 57-58 | ISO-8601 UTC timestamp, seconds precision. |
| `async crawl_page(crawler, url) -> dict` | 65-122 | Builds `CrawlerRunConfig` (71-76: `BYPASS`, 90 s timeout, 3 s delay, `wait_for_images=True`); times `crawler.arun` (78, 83, 89); returns a **page record** `{url, status, ok, elapsed_s, error}` plus a `"markdown"` key. Failure ladder: exception → `error="exception: …"` (84-87); falsy result (91-93); `status in {403, 429}` → `"blocked"` (99-101); `status >= 400 or not success` → `error_message` or `"HTTP n"` (103-106); no `markdown` attr (108-111); `fit_markdown or raw_markdown` empty (113-119). Only the ok path carries markdown (121-122). |
| `save_markdown(url, markdown) -> Path|None` | 125-134 | `PAGE_DIR / f"{slugify(url minus scheme)}.md"`, `write_text`; returns `None` on write error. |
| `write_summary(pages, started_at, finished_at) -> Path` | 137-147 | Builds the summary dict and overwrites `SUMMARY_PATH` (mkdir parent first). |
| `load_urls(path, limit) -> Sequence[str]` | 154-173 | Reads JSON list; accepts `{"url": …}` dicts or bare strings (163); `sys.exit(1)` on missing file / bad JSON / other error (156-169); `urls[:limit]` (171-172). |
| `parse_args(argv)` | 176-185 | `--input PATH` (default `DEFAULT_INPUT`, 178-179), `--limit N` (180-181), `--limit < 1` → `parser.error` (183-184). |
| `async crawl_all(urls, crawler) -> (pages, stopped_early)` | 188-229 | Sequential loop; pause before every page except the first (199-202); pops `"markdown"` from the record (204); on ok → `save_markdown`, downgrade to `ok=False, error="write failed"` if it returns None (206-211); prints one status line per page (212-216); consecutive-blocked counter (207, 217-220); stop at 3 → `return pages, True` (224-227). |
| `async run(urls)` | 232-235 | Opens `AsyncWebCrawler(BrowserConfig(headless=True, user_agent=USER_AGENT))`, delegates to `crawl_all`. |
| `main()` | 238-296 | Parse → load → empty branch writes summary and returns (243-248) → `PAGE_DIR.mkdir` (250) → banner (251-268) → `asyncio.run(run(urls))` (271) → `write_summary` (273) → totals + stats (275-293) → `sys.exit(2)` if stopped early (295-296). |

### Where `result.html` is available, and whether anything touches it

- EVIDENCE — `result` exists only inside `crawl_page`, from line 83 (`result = await crawler.arun(...)`) to line 122. The fields read are `status_code` (95), `success` (96), `error_message` (105), `markdown` (108) and its `fit_markdown` / `raw_markdown` (114-115).
- EVIDENCE — `grep -n "\.html" smart_crawler/crawler.py` → no hits. **Nothing touches `result.html` today.** `crawl_page` returns a plain dict and drops the `result` object; nothing downstream can reach the HTML.
- EVIDENCE — installed crawl4ai 0.9.3 `CrawlResult` model fields include `html`, `cleaned_html`, `fit_html`, `mhtml`, `redirected_url`, `response_headers`, `status_code`, `success`, `error_message`, `url` (`venv/bin/python -c "from crawl4ai.models import CrawlResult; CrawlResult.model_fields"`). INFERENCE: `result.html` is the raw fetched HTML as rendered by Playwright at return time (after the 3 s delay), since `CrawlerRunConfig` has no scraping-strategy override; `cleaned_html` is crawl4ai's post-processed variant. Via the `CrawlResultContainer` `__getattr__` forwarding (recon 2026-09-05, `models.py:311-316`) `result.html` resolves the same way `result.markdown` does.
- EVIDENCE — the test stubs (`tests/test_crawler.py:17-24`) build results with only `status_code, success, error_message, markdown` — **no `html` attribute**. INFERENCE: any new code that reads `result.html` unguarded (`result.html` rather than `getattr(result, "html", "")`) would raise `AttributeError` inside the 7 stub-driven tests.

### `run_summary.json` — where written, exact keys

- EVIDENCE — written by `write_summary` (137-147) to `SUMMARY_PATH = OUTPUT_DIR / "run_summary.json"` (41), i.e. `<repo>/outputs/run_summary.json`. Callers: `main()` line 247 (empty input, `pages=[]`, `started_at == finished_at`) and line 273 (normal path, after `run()` returns, including early stop).
- EVIDENCE — top-level keys, in order (138-144): `started_at`, `finished_at`, `crawl4ai_version`, `pause_range_s`, `pages`. Per-page keys (79): `url`, `status`, `ok`, `elapsed_s`, `error`. Confirmed on disk: `sorted(json.load(open('outputs/run_summary.json')))` → the same five; page keys the same five. The summary contains **no file path, no slug, no byte count, no HTML reference** for a page.

### Exit-code paths

| Exit | Where | Condition |
|---|---|---|
| 0 | implicit | normal completion, including runs with failed/blocked pages that did not hit the 3-consecutive rule; empty input (243-248). |
| 1 | `load_urls` 159, 166, 169 | input file missing / JSON decode error / any other load error. |
| 2 | `main` 295-296 | `stopped_early` (3 consecutive blocked). |
| 2 (argparse) | `parse_args` 184 | `--limit < 1` → `parser.error` → argparse exits 2. EVIDENCE: standard argparse behavior; observed "crawler.py: error: --limit must be >= 1" on 2026-09-05. |
| non-zero, uncaught | `main` 271 | INFERENCE: an exception escaping `run()` (e.g. browser launch failure) is not caught → Python traceback, exit 1, **no summary written** because line 273 is never reached. GAP: not covered by tests. |

## 2. `discover_site/discover.py` (94 lines) and `sitemap_utils.py` (54 lines)

- EVIDENCE — `write_to_json` (`discover.py:50-55`): `data = [ {"url": url} for url in urls ]`. **Each entry is exactly `{"url": <str>}`.** On disk: 194 entries, key set `['url']` (`json.load` over `outputs/discovered_pages.json`).
- EVIDENCE — output path `REPO_ROOT / 'outputs' / 'discovered_pages.json'` (`discover.py:12-13`), root-anchored.
- EVIDENCE — discovery **records no status and no timing per URL**. `fetch_sitemap_urls` returns a flat `list[str]` of `<loc>` texts (`sitemap_utils.py:42-46, 49-50`); `extract_internal_links` returns `sorted(set)` of strings (`discover.py:40-48`). HTTP status is only consulted via `raise_for_status()` on the sitemap/homepage fetches themselves (`sitemap_utils.py:22, 40`; `discover.py:32`) and surfaces as a printed warning, never as data. No timestamps, no source-sitemap attribution, no `lastmod` (the `<lastmod>` elements in the sitemap are ignored — only `ns:loc` is read, `sitemap_utils.py:42, 49`).
- EVIDENCE — order: sitemap mode preserves sitemap order (index children in index order, `35-43`); homepage mode sorts alphabetically (`discover.py:48`).
- EVIDENCE — both HTTP paths go through `SESSION` with `USER_AGENT` (`sitemap_utils.py:7-14, 21, 39`; `discover.py:9, 31`).
- GAP — no dedup across child sitemaps (`urls.extend`, `sitemap_utils.py:43`); no normalisation of trailing slashes; no `--output` flag; discovery is interactive (`input()`, `discover.py:67`).

## 3. `outputs/` — current layout and what a run leaves behind

EVIDENCE (`ls -la outputs outputs/pages`, `.gitignore:177-178` = `outputs/*` / `!outputs/.gitkeep`):

```
outputs/                          (gitignored except .gitkeep)
├── .gitkeep                      0 B, tracked
├── discovered_pages.json         18,548 B — 194 × {"url"}          ← discover.py:13
├── run_summary.json              1,838 B — last run (Cody's, 2026-09-05 15:00) ← crawler.py:41
└── pages/                        10 × <slug>.md, 6,215–29,688 B     ← crawler.py:40, 125-134
```

- INFERENCE — a run leaves: one `discovered_pages.json` (overwritten by each discovery), one `run_summary.json` (overwritten by each crawl, including empty runs), and `pages/<slug>.md` files that are **overwritten in place when the same URL is crawled again and never removed** — `pages/` is cumulative across runs and across sites; there is no per-run folder, no manifest linking a `.md` to the run that produced it, and no record of which `.md` files a given `run_summary.json` refers to except by recomputing the slug (`main()` does exactly that at line 288 for the stats).
- EVIDENCE — nothing else is written: no HTML, no headers, no logs to disk (console only).

## 4. `tests/` — the 14 tests and what each protects

EVIDENCE — `venv/bin/pytest --collect-only -q` → 14 items.

| # | Test | Protects | Breaks if markdown output removed/moved? |
|---|---|---|---|
| 1 | `test_crawler.py::test_403_and_500_write_no_file_and_are_recorded` (57-76) | failure ladder → no file, `error` values, summary top-level and per-page key sets | **YES** — asserts `PAGE_DIR` contains exactly `["example-com-p2.md"]` (69); also pins the summary key sets (73, 76) so *adding* a key to either dict breaks it |
| 2 | `test_three_consecutive_429_stop_the_run` (79-86) | stop rule, 4th URL not attempted, message, no files | **YES** — asserts `PAGE_DIR` empty (86) |
| 3 | `test_blocked_counter_resets_on_success` (89-97) | counter reset | no (only `stopped`/len) — but its ok pages call `save_markdown` into `PAGE_DIR`, so the sandbox `PAGE_DIR` must still exist |
| 4 | `test_pause_between_pages_not_before_first` (100-104) | pause count/placement | no (sleep + stdout only); same `PAGE_DIR` dependency as #3 |
| 5 | `test_limit_and_input_flags` (107-116) | `--input`, `--limit`, default path, `--limit 0` | no |
| 6 | `test_session_user_agent_on_sitemap_calls` (119-135) | UA tokens, Session, no bare `requests.get` | no |
| 7 | `test_import_from_foreign_cwd_creates_no_directory` (138-141) | no import-time filesystem effects | no — unless a new module-level `mkdir` is added |
| 8 | `test_empty_input_writes_truthful_summary_without_crawling` (144-163) | AC-21 empty run | **YES** — pins summary key set (158) and asserts `PAGE_DIR` empty (163); redirects only `PAGE_DIR` and `SUMMARY_PATH` (sandbox 40-41) |
| 9-11 | `test_discover.py::test_is_valid_link_rejects_…`, `…accepts_relative_and_same_domain_only`, `test_normalize_link_…` (7, 15, 21) | URL filter + normaliser | no |
| 12-14 | `test_sitemap_utils.py::test_flat_sitemap_returns_all_locs`, `…index_follows_child_sitemaps`, `…unparseable_sitemap_returns_empty` (25, 33, 41) | sitemap parsing via stubbed `SESSION.get` | no |

- EVIDENCE — the shared `sandbox` fixture (`test_crawler.py:37-50`) knows exactly two output paths: `crawler.PAGE_DIR` and `crawler.SUMMARY_PATH`. INFERENCE: any new output location (an HTML dir, a run folder, a manifest path) that is **not** a module-level constant redirected by this fixture would write into the real `outputs/` during tests — and #7 would fail if it were created at import time.
- EVIDENCE — the stub result (17-24) has no `html`; see §1. Tests #1-#4 and #8 pass the stub through `crawl_page`.
- INFERENCE — "would break if markdown were removed or moved": #1, #2, #8 assert on `PAGE_DIR` contents directly; #1 and #8 additionally freeze both summary key sets with `==`. Moving `.md` files under a run folder while keeping `PAGE_DIR` as the redirected constant that *contains* them would still break #1 (expects the `.md` at the top of `PAGE_DIR`, 69).

## 5. `pytest -q`

EVIDENCE — `venv/bin/pytest -q` → **`14 passed in 2.46s`**.

## 6. Smallest change set to save `result.html` untouched per page into a run folder with a manifest, without breaking the 14 tests (no code)

INFERENCE (design, not implemented): touch **`smart_crawler/crawler.py`** and **`tests/test_crawler.py`** only, plus docs (`README.md`, `RUN_NOTES.md`, `CHANGELOG.md`). In `crawler.py`: add one module-level constant `RUN_DIR_ROOT = OUTPUT_DIR / "runs"` (no mkdir at import, so #7 holds); in `crawl_page` (65-122) read `html = getattr(result, "html", "") or ""` right after `success` and carry it out of the function alongside `"markdown"` as a second popped key (`page.pop("html")` in `crawl_all`, mirroring line 204) — `getattr` with a default keeps the stub-driven tests #1-#4/#8 alive because their results have no `html`; write the HTML bytes verbatim (`write_bytes` of `html.encode("utf-8")`, no reformatting) to `<run_dir>/html/<slug>.html` via a `save_html(url, html, run_dir)` sibling of `save_markdown`, where `run_dir` is created once in `main()` (next to line 250) as `RUN_DIR_ROOT / <started_at-as-filesystem-safe-string>` and threaded into `crawl_all` as a new **optional** parameter defaulting to `None` (so the existing five `crawl_all(urls, fake)` call sites in tests keep their signature and simply skip HTML saving when `run_dir is None`). Keep `PAGE_DIR/<slug>.md` exactly where it is so #1/#2/#8 are untouched. Write the manifest as a **separate file** `<run_dir>/manifest.json` (not new keys inside `run_summary.json`, because #1:73/76 and #8:158 freeze both key sets with `==`) listing per page `{url, slug, md_path, html_path, html_bytes, status, ok}` plus `run_dir`, `started_at`, `summary_path`; write it from `main()` right after `write_summary` (273) and also on the empty-input branch (247) with `pages: []`. Tests: extend the `sandbox` fixture (37-50) to also redirect `RUN_DIR_ROOT` to `tmp_path / "runs"`, add `html="<html>…</html>"` to `_result()` (17-24) as an optional field, and add two or three new tests — HTML saved byte-for-byte for ok pages only, no HTML for failed/blocked pages, manifest present with the right entries, and `run_dir=None` path unchanged — targeting a 17-test suite with the original 14 unmodified. Risk to name now: `slugify` collisions (§1) become data loss for HTML the moment two URLs share a slug; the manifest should record the collision or the slug should gain a short hash — that is a bim001 design call, not a bim000 regression.

---

🛑 Read complete. Read-only. No file changes outside this report.
